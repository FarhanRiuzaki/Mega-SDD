# Audit Phase 1 — Quick Wins (contradiction batch, front-door diet, trace tags, infra)

**Date:** 2026-08-10
**Status:** SHIPPED v6.2.0 (2026-08-10, 43c4b8a, CI green, suite 209/209 both trees) — dual-blind round: 1 blocker + 5 majors + 6 minors, ALL folded pre-ship (§Round disclosure)
**Source:** `docs/superpowers/audits/2026-08-10-skills-audit.md` (14-agent read-only audit, post-6.1.1). This spec ships the audit's Phase-1 roadmap: recommendations #1 (contradiction batch), #6 (front-door diet), #7 (trace-tag completion), #10 (infra batch, trimmed — see Non-goals).
**Version:** 6.2.0 (minor — behavior *clarifications* + content relocation-to-owner + hook latency fix; no gate, grammar, or surface change).

**Rails (unchanged, verbatim from the audit):** nothing here weakens verification. No gate, verdict grammar, halt envelope, keterangan contract, or Mermaid mandate is touched. Every fix picks the already-canonical owner and aligns the drifted copies to it.

## Batch A — the 11 confirmed contradictions (owner → fix)

| # | Contradiction | Owner (wins) | Fix |
|---|---|---|---|
| A1 | `--max-cycles` default 5 (`commands/mega-sdd.md:131`) vs 3 (`convergence-loops.md:33`, self-declared canonical) | convergence-loops.md | Resolved structurally by Batch B: the front door's §Convergence loops section is replaced by a pointer; no default number remains at the front door |
| A2 | `halt-taxonomy.md:19-23` ships 5 admitted classification conflicts (`memory_in_use`, `mode_migrate`, `invalid_handoff`, `partial_state_corrupt`, `verify_unit_writable`) vs the canonical registry | `references/halt-protocol.md` (verified: all 5 are C1 self-resolve / detection-only, NEVER-halt; `invalid_handoff` escalates C2 on 2nd consecutive failure) | Move the 5 names out of §Always-stop into a new names-only §Self-resolve (C1) list mirroring the registry; delete the ⚠ annotations |
| A3 | Staging-drop severity: template teaches HALT (`generate-intent templates/04-flows.md:66`) vs advisory at both teachers (`vault-contract.md:128`, `generation-guide.md:32`) | the teachers (v4 Hybrid demotion) | Template comment now says "a drop is an **advisory** `vault_flow_staging_drop` finding (surfaced via `analyze`)" |
| A4 | Express default fork: bind `SKILL.md:47` treats `--express` as flag-conditional; `express-bind.md:3` says "the DEFAULT"; `interactive-walk.md:650` tells users "(default…)" | the lane fact: the CHAIN injects `--express` (spine default); a bare standalone invocation runs the classic lane unless `--express` is passed | One clarifying sentence in bind §Inputs; `express-bind.md:3` rephrased to "the lane the express spine dispatches by default"; `interactive-walk.md:650` drops the "(default" claim |
| A5 | detect-drift's commanded write-back ref still teaches the REMOVED interactive ACCEPT flow (`report-format.md:135-152` DRIFT-ACTIONS template, `:171` Batch-confirm UX, `:173` On-ACCEPT, `:177`); `SKILL.md:70` Step 6 branches on "Walkthrough…/(Step 5.5 ACCEPT)"; `SKILL.md:100` still advertises the DRIFT-ACTIONS template; `sync-digest.md:21-22` example says "awaiting ACCEPT" + "proposed_patch in DRIFT-ACTIONS.md" | the fork contract (v3.0.0, test-pinned) | Rewrite §Vault write-back protocol non-interactively (deterministic `--auto-apply=safe` selection; out-of-class drafts preserved as `proposed_patch:` in the PENDING-SYNC.md entry); DELETE the §DRIFT-ACTIONS.md structure section (the header's **deprecated (v3.0.0)** notice stays VERBATIM — `tests/drift/test-detect-drift-fork.sh:75` pins the phrase); fix SKILL Step 6 branch labels + the :100 roster; sync-digest heading → "awaiting human triage", patch home → PENDING-SYNC.md entry |
| A6 | scan trio: ast-grep dedupe key `(file, start.line)` (`tree-sitter-integration.md:127-128`) vs owner `(file, range.start.line, ruleId)` (`scan-procedure.md:434` — warns the 2-tuple drops real definitions); "The four subagent prompt templates" (`deep-scan-gate.md:9`) vs five; `cache_signatures` labeled v2.0 (`deep-scan-gate.md:80`, `deep-scan-dispatch.md:187`, `:308`) vs the block's own v2.1 (`:263`) | scan-procedure.md §Step 5; the template roster (5); the v2.1 block | Three one-line corrections + label unification (the starterkit `schema_version: 3.1` is a different version and is untouched) |
| A7 | Residual `expects` poison (the 6.1.1 field-defect class): `decomposition-rails.md:160-172` provenance example shows `description:` entries with no `expects:`; `pbt-integration.md:30-31` shows `expected_exit_code: 0` (field does not exist — the matcher is `rc==0 AND (expects empty OR substring)`) | `unit-schema.md` substring contract (6.1.1) | Both examples rewritten to the canonical `command` + `expects: ""` shape; the gap-merge annotations become YAML comments. The `_authored_by:` key KEEPS its documented non-strict mapping-in-sequence shape — it is a real key in real units, read by a standalone regex before any structured parse (`build-dispatch-prompt.sh`), so the example must show the real grammar (round disclosure) |
| A8 | `templates/unit.md` frontmatter omits `task_type` / `grounding_confidence` / `module` (unit-schema "Required frontmatter") and renders `## Context` as bullets where the schema commands "## Context (read first) … Conversational directive prose, NOT bullets" | unit-schema.md | Template gains the three required keys (terse comments) + the Context section matches the schema shape. The 6.1.1 `expects` line is byte-identical (test-p8 pin). Full body-section census (Anchors/Hard rules/…) → the Phase-2 schema↔template sweep, not this patch |
| A9 | Memory config path: `~/.mega-sdd/config.yaml` (`memory/SKILL.md:100`, `memory-schema.md:625`) vs `~/.mega-sdd/memory/config.yaml` (`learning-rules.md:25`, `memory-schema.md:243` — the schema itself) | the schema's path `~/.mega-sdd/memory/config.yaml` (no script reads either — prose-only surface, verified) | Fix the two stray citations |
| A10 | Extract emit-verbatim templates stamp `extract-intelligence@1.6.0` / `@1.11.0` (`SKILL.md:220`, `:257`) while the skill is 1.18.x | frontmatter `version:` | Both values become the placeholder `extract-intelligence@<skill-version>` with a fill-in instruction (matching the blocks' other `<...>` fill-ins) |
| A11 | Misc trio: diff-vault version-bump heuristic ×3 with inverted vocabulary (`diff-procedure.md:92-93` labels v1.1→v2.0 "Minor"); duplicate pre-flight step "5." (`execute-bolts/SKILL.md:60-61`; no ordinal citations exist — verified); `design-reviewer.md:9` + item 9 promise "traits/anti-patterns" vocabulary the builder is FORBIDDEN to emit (`context-enrichment.md:335-345`) | diff-procedure.md §Update vault metadata (single owner, new unambiguous vocabulary: **small bump** vX.Y+1 / **scope bump** vX+1.0, ≥5-entities threshold as the deterministic tiebreak); ordinal sequence; the delivered slice vocabulary (best-for / avoid-for / CSS keywords + baseline digests) | Rewrite the three bump sites consistently (SKILL:68 + auto-and-chain:27 point at the owner; keterangan content preserved, labels renamed); renumber the PBT pre-flight to 6; reword design-reviewer's two lines to the actual prompt contract |

## Batch B — front-door diet (`commands/mega-sdd.md`)

The file declares "it never duplicates chain logic" (:8) then carries three chain-logic blocks owned by orchestrate-flow refs. Cuts (each becomes a 1-3 line pointer; front-door-specific content stays):

- §Starterkit detection: keep the user directive, the halt + its options, and the dispatch line; the 3-mode × 2-spine table lives ONLY in `orchestrate-flow/references/chain-execution.md` §Starterkit detection + mode classification (round correction — the first cut pointed at routing-rules §Decision matrix, which carries the STATE-based chain rows, not the mode table; both blind reviewers caught the misroute). **Pre-cut merge check:** any row present here but missing in the owner's table is merged into the owner first (relocate-then-delete) — verified: the owner is a superset.
- §Auto-integrated diagnostics: keep the opt-out/opt-in flag lists (flag law is front-door duty); the phase table lives ONLY in `chain-execution.md` §Auto-integrated diagnostics — same pre-cut merge check (this table names lanes by their CURRENT routing, so any stale `lint-units`-style skill name dies with the cut).
- §Convergence loops: replaced by a 3-line pointer (mechanics + eligibility + cap default live in `convergence-loops.md`; classification canon = halt-protocol.md). This deletes the stale 9-entry always-stop list (`:138`, which contradicted halt-taxonomy on `mode_migrate`), the divergent default-5, and the `pre-v3.12` archaeology.

## Batch C — trace-tag completion (observability contract)

The anchor mandates the tag on every announce line and every subagent dispatch prompt; fresh-context surfaces cannot follow a pointer they never see. Add `mega-sdd-trace:<skill>`:

- Announce lines: EVERY SKILL.md carrying an `**Announce at start:**` template — the 4 emit skills PLUS (round widening, reviewer-1 MAJOR: "completion" must mean completion) bind-codebase, execute-bolts, extract-intelligence, generate-units, install-deps, memory, orchestrate-flow, scan-codebase (emit-uat already carried it; skills with no announce template rely on the anchor rule).
- Dispatch templates: `extract-intelligence/references/wave-dispatch-templates.md` (tag line inside the per-wave dispatch prompt), `scan-codebase/references/deep-scan-prompts.md` §Common dispatch contract (single owner — `deep-scan-dispatch.md` routes its prompts through those templates and inherits the rule; round correction, no second copy added), `execute-bolts/references/review-panel.md` §Blind dispatch (lens + verifier prompts) — exactly as `build-dispatch-prompt.sh` already does for bolt dispatches.

## Batch D — infra batch (trimmed)

- **D1 user-prompt-submit fast path:** port pre-tool-use's pure-shell fast negative short-circuit (`[[ =~ ]]` last-match cwd loop + `resolve_project_root` + `.mega-sdd` existence check, fall-through on extraction failure) BEFORE the python3 stdin parse. Removes 1 bash + 1 python3 spawn per user prompt in every non-mega-sdd project (~30-150ms macOS, ~220ms+/spawn on the Windows/CrowdStrike fleet). Moat-neutral by the same construction as the sibling hook.
- **D2 non-SDD slim routing block halved:** `hooks/session-start` no-signal branch payload cut from 834 B to **468 B measured** (round-corrected figure; first cut landed at 513 B and both reviewers flagged the spec/tree gap) — keep the Hard rule (incl. the casual-session exemption sentence, which is load-bearing) + the opt-out pointer; drop the Output-language paragraph (the full anchor re-teaches it the moment a pipeline starts). Product decision (mandatory-workflow routing) unchanged; proof pin < 550 B.
- **D3 anchor-core headroom:** deliberate diet of `using-mega-sdd` ANCHOR-CORE from 3,598 B (2 B under the 3,600 CI cap) to ≤ ~3,400 B — trim the legacy-path signal list the resolver already handles; NO trigger keyword and NO hard-rule sentence moves below the marker. Tighten `tests/token-efficiency/test-b3-anchor-and-panel.sh` cap to the new ceiling so the margin is enforced, not accidental.
- **D4 tool-matrix `used_by` rot:** `jd` → `[diff-vault, execute-bolts]` (replay procedure — `execute-bolts/references/replay.md:91` — plus diff-vault); `markdownlint-cli2` → `[orchestrate-flow]` (the lint lane — `diagnostics-procedures.md` Step 5). Extend `tests/derived-artifacts/test-tool-matrix.sh` to assert every `used_by` entry resolves to a live skill directory.
- **D5 tombstone:** delete `scan-codebase/references/deep-scan-stage.md` (247 B relocation note; zero inbound content links — verified).

## Proof

`tests/surface/test-p9-audit-phase1.sh` (new): pins the fixed pairs — taxonomy carries zero ⚠ conflict markers and the 5 names sit in the C1 list; the front door carries no `--max-cycles (default`, no starterkit mode table, no diagnostics phase table; 04-flows says advisory; report-format has no Batch-confirm/On-ACCEPT and keeps `deprecated (v3.0.0)`; both expects-poison examples carry `expects: ""`; template frontmatter has the three keys; config path unified; extract placeholders present; announce lines + dispatch templates carry their trace tags; user-prompt-submit fast path precedes the python parse; anchor cap tightened. Existing suites re-run in full (both trees).

## Round disclosure (dual-blind, 2 reviewers, read-only + mktemp)

Reviewer 1 (spec-fidelity): 0 blocker / 5 major / 2 minor. Reviewer 2 (breakage + moat; hook fast-path sandbox-executed, moat verified untouched): 1 blocker / 1 major / 6 minor. ALL folded pre-ship: the stale `test-p2-ground-express-default.sh` pin on the killed express-bind phrase (blocker — pin moved WITH the phrase + gained the lane-default arm); the front-door starterkit owner misroute (both reviewers); walkthrough-era vocabulary surviving in `routing-rules.md:96/:105` (+ the D7 delivery pin moved with the rewording); the abolished bump vocabulary re-shipped/surviving in `interactive-walk.md`, `detect-drift/SKILL.md`, `report-format.md` (all now cite the diff-procedure owner grammar); trace-tag completion widened to all 12 announce-bearing skills; `convergence-loops.md` re-anchored to the four-class taxonomy; the `mode_migrate` explicit-`--mode` override carve-out added to the C1 list; slim block trimmed 513→468 B; spec figures/owners corrected per the tree (this section + the amendments above). Round lessons re-confirmed: pins move WITH phrases; "single owner" claims must name the file that actually holds the content; a "completion" batch is measured against the mandate, not the flagged subset.

## Non-goals (explicitly deferred, with owners)

- **styling-config.yaml dead-knob cut** — requires a deliberate emission-parity re-baseline (`test-p3-emission-parity.sh`); Phase 2.
- **ci-recipe.md / AUDIT.md relocation** — ci-recipe is pinned in place by `tests/roadmap/test-roadmap-pins.sh §D` and costs zero context; Phase 3 housekeeping.
- **Repo-wide archaeology purge, WHEN-triggered ref loading, single-owner sweeps + parity tests, script-ification, sync short-circuit** — Phase 2 (own spec + round each).
- **Blank emit-description verification** — needs a fresh interactive session (user action).
- `mega-sdd.md:108`-style stale lane names inside the deleted diagnostics table are NOT individually patched — the table is deleted; the owner table's naming stands.
