# W-batch — the write-lane exits the model (script-derived artifacts)

**Date:** 2026-07-19 · **Research:** [`research/2026-07-19-token-format-research-okf.md`](../../../research/2026-07-19-token-format-research-okf.md) · **Verdict basis:** 4 spec-draft agents + 1 cross-check auditor, every claim verified file:line against v4.81.0.

**Goal:** the model stops WRITING derived artifacts (output tokens ×5); deterministic scripts derive them instead. Every item either preserves or **strengthens** a gate — two items close live forgery holes. No format migration (rejected by research — TOON/compression dull the moat).

## Ship order (one green commit each, both test trees, marketplace.json == plugin.json)

| Phase | Item | Version | Why this order |
|---|---|---|---|
| 1 | **W4** preflight.json script-written + hook-guarded | v4.82.0 | File-disjoint from all others; closes a VERIFIED live B1 forgery hole (`scan_unit` prefers a present preflight sha/signature snapshot over commit evidence — `_lib/postflight_rules.py:382-399,456-470` — and `preflight.json` matches NO existing write guard) |
| 2 | **W3** citation-map script-computed | v4.83.0 | File-disjoint from bind surface; drives model-written hash characters to zero — a fabricated hash becomes impossible (script computes from file bytes; fabricated path → deterministic halt) |
| 3 | **W2** binding.json script-derived from binding.md | v4.84.0 | Must precede W1 (rewrites bind SKILL Step 4.5 adjacent to W1's Step 5; owns the `_lib/binding_md.py` extraction W1's parity preflight consumes) |
| 4 | **W1** bound/ script-produced | v4.85.0 | Lands last over W2's settled surface; kills the largest single re-type (~10–30k output tok/bind) with zero extra model output (annotations derive from binding.json `vault_source`) |

## Decisions locked (owner calls resolved per draft recommendations)

- **W1:** annotation grammar `<!-- BIND: <verdict>=<claim-id> -->` (OQ rows use claim-id form `oq=C-012` — nothing parses either form; contract-pinned by the new tests). KEEP the `bound/binding.md` mirror (byte-cp is token-free; dropping is a contract change beyond W1). NO PreToolUse Write-deny on `bound/**` (non-moat state — doctrine: don't grow the hot-path surface; bound/ existence cannot open the CONFLICT gate, verified). Stale-bound semantics unchanged (refusal touches nothing). `--strict` is passed by the model; risk documented.
- **W2:** missing `## Implementation State Map` heading → exit 2. `schema_version` stays `"1.0"` (key set unchanged; nothing consumes it). Add one grep-level WARN in `validate-conflict-classification.sh` when an ACTIVE conflict block lacks `- **Claim**:`. resolve-oq re-derive does NOT re-run the parity validator (tautological). `generated_at` preserved when derived content is otherwise identical (idempotent re-derive, no git churn). **The three new bind-time md-grammar obligations MUST be authored in the same phase:** (1) `binding_metadata.head` in binding.md frontmatter; (2) `[reason: <enum>]` token grammar in the State Map Anchor cell (closed enum `truncated_section|ambiguous_match|dynamic|regex_tier|kb_confirmed`); (3) `- **Claim**: C-NNN` line in `### CONFLICT-N` detail blocks (mandatory on RESOLVED, recommended on ACTIVE).
- **W3:** `--sections` subset keeps parity with current behavior (no merge-prior). `emitted_text_sha256` kept, redefined under schema 2.0 (section byte-slice hash). In-doc stamp cross-check and `(commit:)` git verification deferred (out of scope). Stdout grammar pinned: `DRIFT <section> <path> <old12> <new12>` / `GONE <section> <path> <old12>` / `UNVERIFIED <section> <path>` (1.0-fallback) / `NO_PRIOR` / `PRIOR_UNREADABLE`; exit 0 for all informational outcomes, 2 usage. `build-citation-map.sh`: exit 0 clean, 1 unresolved citations (map still written, `unresolved: true` entries; halt `quality_gate_failed` subtype `citation_unresolvable`), 2 usage/missing FSD.md. Doc-header vault stamp = sha256 of `<vault>/vault.json`, special-cased on the `**Source vault:**` line.
- **W4:** exit 7 (post-hoc refusal — bolt commits exist, no baseline) is non-fatal: proceed, post-flight falls back to commit evidence, log in bolt-report. Mixed-grammar pre-flight stays doc-literal (halt) with the existing `--grammar` override as escape hatch. `deps_section` still captured (audit parity). Shell PROTECTED pattern uses ERE `(pre|post)` capturing group (BSD grep -E has no `(?:`).
- **Tests:** top-level functional tests for W1/W2/W3 consolidate into `tests/derived-artifacts/`; W4's two tests join existing `tests/postflight-evidence/`. Plugin-tree tests: W1 `tests/moat/test-make-bound-gate.sh`, W2 `tests/graph/test-derive-binding-json.sh`, W3 `tests/moat/test-citation-map-script-owned.sh`. All invocations `</dev/null`. Suite: 129→137 discovered (134 executed after the 3 quarantined pack suites). Must-stay-green-unmodified: `plugins/mega-sdd/tests/graph/test-binding-json-parity.sh`, `tests/god-review-s4/test-4d-contract-truth.sh`.

## Full per-item specs

The complete drafts (current-behavior evidence, edit lists, script algorithms, test asserts, risks) live in the session workflow output (`wf_75db6766-8a0`); the implementation of each phase follows its draft verbatim except where a Decision above overrides. Key interfaces:

- `scripts/run-preflight-scan.sh --cwd=<root> --unit=U-XXX` — imports `_lib/postflight_rules.py` primitives (extract_hard_rules, sha256_of, find_decl_line, walk_unit_commits); exit 0 written/kept/no-rules, 2 usage, 3 unparseable, 4 mixed, 5 unanchored, 6 dep_missing, 7 post-hoc refusal. Hook guard: 3 regex extensions in `hooks/pre-tool-use` (lines ~723/845/872) covering `(pre|post)flight.json`.
- `scripts/build-citation-map.sh --vault=<vault> --cwd=<root> --mode=<mode>` + `scripts/check-citation-drift.sh --vault=<vault> --cwd=<root>` — model emits `(sha256: pending)` placeholders only; builder stamps real 12-char prefixes in FSD.md (binary-safe, atomic, before pandoc) and writes schema-2.0 map; drift reader prints only diff lines. Path recognition reuses `_lib/citation_pattern.py`.
- `scripts/derive-binding-json.sh --vault=<vault>` + NEW `scripts/_lib/binding_md.py` (absorbs `validate-binding-json.sh`'s parser byte-identical; shared by validator + generator — the B1 shared-engine precedent).
- `scripts/make-bound.sh --vault=<vault> [--strict]` — parity preflight via `validate-binding-json.sh`; REFUSES (exit 2, fs untouched) while any CONFLICT (or OQ under --strict); temp-dir production + atomic swap; annotations from binding.json `vault_source` (skip null/section-style/out-of-bounds — no fabrication; skips counted in summary).


## Amendment 2026-08-30 (spec `2026-08-30-audit-driven-hardening.md` §1.2, F-06)

W4's residual — "a FORGED post-tamper baseline passes the engine, so the write guard + post-hoc refusal are load-bearing" — is CLOSED for `DO_NOT_MODIFY` on committed bolts: `scan_unit` now judges by the unit's OWN commit touched-set first and treats the snapshot as a note. Field measurement behind the flip: 6 `DO_NOT_MODIFY` halts on a 36-unit run, all false positives (sibling units changing a shared file), 0 true positives, 15 baselines latently stale. The guard, the post-hoc refusal (exit 7) and the tamper-before-mint refusal (exit 8) stay: they remain load-bearing for `SIGNATURE_RULE` (still snapshot-keyed) and for the pre-commit working-tree mode. Pin: `tests/postflight-evidence/test-preflight-scan.sh` §I (repinned) + `test-do-not-modify-touched-set.sh`.
