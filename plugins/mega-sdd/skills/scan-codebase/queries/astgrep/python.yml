# Tier-1 AUTO-lane (ast-grep) definition rules — Python (.py).
# Coverage mirrors the former tags-python.scm set (tree-sitter lane removed 7.4.0; ast-grep is the only engine) (decorated defs match via their inner
# definition node). Kind names verified against ast-grep 0.42.3.
# Contract: 0-based lines (+1 for anchors); dedupe by (file, start.line, ruleId).
id: python-function
language: python
rule: {kind: function_definition}
---
id: python-class
language: python
rule: {kind: class_definition}
