# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Test outputs (skill-generated)
test-output/
*.test.md
!tests/**/*.test.md
!plugins/mega-sdd/tests/**/*.test.md

# Archive
*.zip
*.tar.gz

# mega-sdd runtime state files (generated by validators, never committed)
**/.mega-sdd/.*-state.json
**/.mega-sdd/factory-ledger.json
!tests/fixtures/**/factory-ledger.json
**/.mega-sdd/.*-blockers.json
**/.mega-sdd/.validation-blockers.json
**/.mega-sdd/.plan-pending
**/.mega-sdd/.replan-budget
**/.mega-sdd/.iter-classifier.json
**/.mega-sdd/.analyze-state.json
**/.mega-sdd/.analyze-freshness.json
**/.mega-sdd/CONSISTENCY-REPORT.md
**/.mega-sdd/RULE-GAP-REPORT.md
**/.mega-sdd/memory/hook-debug.log
**/.mega-sdd/memory/telemetry.jsonl
**/.mega-sdd/.compaction-snapshot.json
**/.mega-sdd/TOKEN-COST-REPORT.md
**/.mega-sdd/graph.json
**/.mega-sdd/state.json
**/.mega-sdd/project.md
!tests/fixtures/**/.mega-sdd/state.json
!tests/fixtures/**/.mega-sdd/graph.json

# Internal training material (not for the public repo)
training/

# Python bytecode cache (scripts/_lib import cache)
__pycache__/
*.pyc

# Env
.env
.env.local

# mega-sdd derived caches + scan stamps (tranche 4 — derived, never state)
**/.mega-sdd/.cache/
**/.mega-sdd/.stop-scan-stamp
**/.mega-sdd/.ptu-scan-stamp

# mega-sdd runtime residue (v7 Fase 2 tree hygiene): per-session usage cursors +
# rotated telemetry generations — same class as telemetry.jsonl above
**/.mega-sdd/memory/.turn-usage-cursor-*.json
**/.mega-sdd/memory/telemetry.jsonl.1
**/.mega-sdd/memory/hook-debug.log.1

# Local tooling residue — NOT part of this repo:
# mega-code CLI npm-installed in-tree for gateway field tests, and the
# playwright-MCP session logs from UAT evidence runs
/node_modules/
/package.json
/package-lock.json
/.playwright-mcp/

# Public-repo hygiene (8.4.2) — never tracked:
# per-machine Claude Code permission allowlist
.claude/settings.local.json
# local session notes
Claude outputs/
# raw benchmark run logs — full `claude -p` stream + stderr (tens of MB; carry the
# author email + local paths from `git log` output). The measured summaries next to
# them (extract.json, run.meta, quality.json, vault-snapshot/) stay tracked.
benchmarks/results/**/stream.jsonl
benchmarks/results/**/stderr.log
