---
unit: U-011
status: success
attempted_at: 2026-09-14T14:13:18+00:00
duration_seconds: 558008
commits: [6bd8691415b0794cd291f54107112e14cc77f617]
files_touched: [src/app/(patient)/layout.tsx, src/features/appointments/components/PatientShell/index.test.tsx, src/features/appointments/components/PatientShell/index.tsx]
tests_run: ["pnpm test:run src/features/appointments/components/PatientShell/index.test.tsx --reporter=verbose"]
test_results: "0 acceptance entries passed / 4 not passing (see acceptance.json)"
retries: 0
target_hashes:
  src/app/(patient)/layout.tsx: 1e005bf7f84e2f0ef4582598e5b9b74b06cb39d64ca32a88ee90ee0a52e2f02b
  src/features/appointments/components/PatientShell/index.tsx: 1453a88a5f2eabc6dad2da5753d55dc9a3edcf654951a4271100f6ff7df54bc9
  src/features/appointments/components/PatientShell/index.test.tsx: 4559f093e017b070854d1ea4eac43439308e004ec7fbc306b2bdebf370887ff6
---

# Bolt Report — U-011

## Summary
(see implementer report)
# Bolt report — U-011 "Add the branded patient shell layout"

- **Status:** DONE_WITH_CONCERNS
- **Commit:** 6bd8691 `feat(U-011): Add the branded patient shell layout` (trailers: `Unit: U-011`, `SDD-PROVENANCE: mega-sdd/execute-bolts unit=U-011`, `SDD-Acceptance: v5`)

## What was implemented

- `src/app/(patient)/layout.tsx` (create): async server layout that follows the blank-layout anchor. It wraps children in `<Providers direction='ltr'><PatientShell>…</PatientShell></Providers>` and sets `metadata.title = { template: '%s — Clinic Appointment System', default: 'Clinic Appointment System' }`. The anchor's `BlankLayout`/`getSystemMode` were not copied because nothing here would use them.
- `src/features/appointments/components/PatientShell/index.tsx` (create): client component with:
  - a plain `Box` root. Not a sectioning element, so `header`/`footer` keep the `banner`/`contentinfo` roles.
  - a skip link to `#main-content`, off-screen until it receives focus.
  - a `<header>` holding the heading-styled text brand "Clinic Appointment System" (a `Typography variant='h5' color='primary'` link to `/book`, not an `<h1>`) and `<nav aria-label='Primary'>` with a single "Book an appointment" link to `/book`. When active, that link gets `aria-current='page'`, primary color and an underline bar, so the state is not shown by color alone.
  - a `<main id='main-content' tabIndex={-1}>` whose content sits in a `Container maxWidth='lg'` with responsive padding.
  - a `<footer>` with `© <current year> Clinic Appointment System`.
  - every link with a `:focus-visible` 2px primary outline and a minimum block size of 44px, a flex-wrapping header, no fixed widths, no `nowrap`, and a hover transition that turns off under `prefers-reduced-motion`.
- `src/features/appointments/components/PatientShell/index.test.tsx` (create): three tests named exactly as the `acceptance_test.expects` strings.

## Tests

- TDD: I wrote the test first and ran the acceptance command. It failed for the right reason: `import PatientShell from '.'` could not resolve because the module did not exist yet. Then I implemented.
- `pnpm test:run src/features/appointments/components/PatientShell/index.test.tsx --reporter=verbose` gives 3/3 passed:
  - `renders header main and footer landmarks` ✓
  - `links the header navigation to the booking page` ✓
  - `shows the product name as the brand text` ✓
- Full suite `pnpm test:run`: 16 files / 90 tests passed.
- `pnpm exec eslint` on the three files: no issues. `pnpm exec tsc --noEmit`: no errors.
- **Manual acceptance entry (375px, keyboard reachability, visible focus ring): NOT verified.** It can't be checked in jsdom and `/book` does not exist until U-013 lands. The code supports it (wrapping header, `Container lg` with `px: { xs: 4, sm: 6 }`, no fixed widths, a focus-visible outline on every link), but a human must open `/book` at 375px to confirm.

## Hard rules honored

- DO NOT add new package.json dependencies: honored (no manifest touched).
- DO NOT modify src/components/Providers.tsx: honored (imported only).
- file src/app/(patient)/layout.tsx MUST exist after bolt: honored (created in 6bd8691).
- MUST render semantic header, main and footer landmarks with a keyboard-reachable navigation and visible focus (constitution F-001): honored. The landmarks are asserted by test; the focus ring is in code but not visually verified (manual).
- MUST NOT show a logo image or clinic name that the PRD does not provide (context.md OQ-CN-5): honored. The brand is the PRD product name as text and the test asserts no `img`. The only icon is a decorative `tabler-calendar-plus` on the nav link, not a logo.
- MUST stay usable at a 375px viewport without horizontal scrolling (source: PRD §Clinic.2 NFR-001): implemented through responsive layout; manual verification is pending.

## Provenance trailer

All three files carry the `Generated by mega-sdd execute-bolts v7.37.1` marker, the unit/sha line, the anchors and the six hard-rule texts verbatim. **The `Implements claim:` line is OMITTED** because the dispatch Provenance values block gives `claims: (none cited)`.

## reuse_decisions

```yaml
reuse_decisions:
  - candidate: reuse-index.yaml (full scan)
    decision: not_applicable
    reason: "reuse-index.yaml is absent at .mega-sdd/codebase/reuse-index.yaml (recorded in the dispatch PROVENANCE appendix); nothing to scan. I looked for existing shells/links by grepping the repo instead."
  - candidate: src/components/Link.tsx (app forwardRef wrapper over next/link)
    decision: reused
    reason: "Serves as the `component` for every brand/nav/skip link, so MUI Typography styling applies to a Next client-side link."
  - candidate: src/components/Providers.tsx
    decision: reused
    reason: "Wrapped unchanged by the (patient) layout, as the anchor requires."
  - candidate: "@layouts/BlankLayout / vertical FooterContent (template)"
    decision: not_applicable
    reason: "BlankLayout gives no header/nav/footer furniture. FooterContent is tied to the vertical admin nav (useVerticalNav) and carries template vendor links."
  - candidate: "@/test/utils renderWithProviders + within"
    decision: reused
    reason: "Test harness, as specified in step 3."
```

## Self-assessment

```yaml
acceptance_test_concern: >
  The three tests check structure only: landmark roles, the nav href, the brand text and that no img
  is rendered. They do not cover the 375px no-horizontal-scroll requirement, focus visibility, the
  active-link state (usePathname is mocked to '/' globally), the footer year, or the skip link.
  Two assertions I would add: (1) the nav has the accessible name "Primary" and the skip link's href
  is "#main-content", which matches main's id; (2) the footer contains the current year from
  new Date().getFullYear(). Active-state coverage would need a per-test usePathname override that
  returns '/book' and a check for aria-current='page'.
truncation_note: >
  The dispatch design_slice was truncated (modern-baseline to lead clauses, ux-rules to High rows).
  Design choices resting on it are therefore MEDIUM confidence.
```

```yaml
bolt_self_report:
  model_used: "Opus 5"
  confidence: 0.8
  certain_decisions:
    - "Test names match the acceptance_test expects strings verbatim; all three pass."
    - "Layout wraps Providers unchanged and imports no unused anchor symbols; Providers.tsx untouched."
    - "Brand is heading-styled text (Typography h5 rendered as a link), not an h1, so the page (U-013) keeps its own h1."
    - "Root wrapper is a plain Box, so header/footer map to banner/contentinfo."
    - "Brand/active link use theme primary, which U-004 set to teal-700 #0E7490 (5.36:1 on white)."
  uncertain_decisions:
    - decision: "Added a skip-to-main-content link, off-screen until focused"
      rationale: "The spec's id='main-content' otherwise has no consumer; a skip link supports F-001 keyboard reachability (WCAG 2.4.1)."
      fallback_if_wrong: "Delete the skip-link Typography block; the landmarks and nav still satisfy the tests."
    - decision: "Decorative tabler-calendar-plus icon (aria-hidden) on the nav link"
      rationale: "Iconography ceiling move; it is not a logo, so OQ-CN-5 is not violated."
      fallback_if_wrong: "Remove the <i> element from NAV_ITEMS rendering."
    - decision: "Primary-colored brand text in dark mode"
      rationale: "The theme keeps primary.main #0E7490 in dark mode too (src/components/theme/index.tsx). Teal text on the dark paper surface is likely below 4.5:1. Fixing it belongs to the theme (U-004 scope), not this unit."
      fallback_if_wrong: "Theme owner sets a lighter dark-mode primary.main, or the shell switches to text.primary in dark mode."
  retry_history:
    - attempt: 1
      failure: "TDD red: Failed to resolve import '.' from index.test.tsx (PatientShell not yet created) — expected failure"
      fix: "Created PatientShell/index.tsx; 3/3 passed on the next run."
```

## Concerns

1. The manual 375px / keyboard / focus-ring acceptance is unverified until `/book` exists (U-013).
2. Possible dark-mode contrast shortfall for primary-colored text. The theme owns this; it is out of scope for this unit.
3. The `/reschedule/[token]` route will also sit under this layout (U-014). Its nav has no reschedule item because the spec lists only "Book an appointment".

## Post-commit verifications (no action needed)

- **Icon availability:** `src/assets/iconify-icons/bundle-icons-css.ts:68-70` bundles the whole `@iconify/json/json/tabler.json` (no `icons` filter), so `tabler-calendar-plus` ships once `pnpm build:icons` has run. The generated CSS is not present in this working tree, which doesn't matter because tests run with `css: false`.
- **Commit trailer shape:** the body of 6bd8691 has the same shape as the gate-cleared U-012 commit a0aef4a (`Unit:`/`SDD-PROVENANCE:`/`SDD-Acceptance:` block, blank line, `Co-Authored-By:`). No amend needed.
- **Anonymous reachability:** `src/proxy.ts:23-29,45-47` (U-010) already lets `/book` and `/reschedule/<token>` through without a session, so the `(patient)` shell is reachable by patients. The manual 375px check can run once U-013 adds `/book/page.tsx`.

## Rollback hints

```yaml
- step_id: step-1-test-created
  step_type: file_created
  evidence: "created src/features/appointments/components/PatientShell/index.test.tsx (58 lines)"
  compensating_action: "git rm src/features/appointments/components/PatientShell/index.test.tsx"
  idempotent: true
- step_id: step-2-layout-created
  step_type: file_created
  evidence: "created src/app/(patient)/layout.tsx (46 lines)"
  compensating_action: "git rm 'src/app/(patient)/layout.tsx'"
  idempotent: true
- step_id: step-3-shell-created
  step_type: file_created
  evidence: "created src/features/appointments/components/PatientShell/index.tsx (183 lines)"
  compensating_action: "git rm src/features/appointments/components/PatientShell/index.tsx"
  idempotent: true
- step_id: step-4-tests-run
  step_type: test_command_run
  evidence: "pnpm test:run PatientShell/index.test.tsx → 3/3 passed; full suite 90/90"
  compensating_action: "(none — manual review required)"
  idempotent: true
- step_id: step-5-commit
  step_type: git_commit
  evidence: "6bd8691 feat(U-011): Add the branded patient shell layout"
  compensating_action: "git revert --no-edit 6bd8691"
  idempotent: false
```

## Review panel
- Tier: **full** · signals_fired: ['auth_globs'] · lenses: spec, quality, security, standards · implementer model routing: inherit (session model)
- L0: run-code-gates.sh --write per unit commit (6bd8691) — no blocking finding
- Ledger `findings.json` (merge-panel-findings.sh): attempt 1 · gate clear · open 0 · advisory 5 · resolved 0

| id | severity | file:line | lens | status | title |
|---|---|---|---|---|---|
| F-1 | Important | src/features/appointments/components/PatientShell/index.tsx:32 | quality | advisory | yagni: one-item NAV_ITEMS + map + active-link logic makes the shell a client component |
| F-2 | Minor | src/app/(patient)/layout.tsx:26 | quality | advisory | Product name hardcoded 3 times across the two new files |
| F-3 | Minor | src/app/(patient)/layout.tsx:33 | quality | advisory | async layout that awaits nothing |
| F-4 | Minor | src/features/appointments/components/PatientShell/index.tsx:88 | design | advisory | Header/footer are flat (border-only, no elevation) — weak depth cue |
| F-5 | Minor | src/features/appointments/components/PatientShell/index.tsx:32 | design | advisory | Nav has a single item, header composition is sparse |

## Post-flight
- Hard rules: postflight.json status **pass** (DO_NOT_ADD_DEPS=pass, DO_NOT_MODIFY=pass, FILE_PRESENCE_RULE=pass)
- Acceptance: acceptance.json status **pass** (0/3 executed entries pass)
- ✓ Drift check: clean (lite lane — no binding.md anchors; no LOCKED entities in the vault)

