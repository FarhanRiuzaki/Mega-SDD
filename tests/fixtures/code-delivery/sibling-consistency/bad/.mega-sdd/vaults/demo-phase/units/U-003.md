---
unit_id: U-003
title: gizmos migration + Gizmo model
task_type: create
phase: 1
module: catalog
scope: branch-scoped
loc_budget: 150
---

# U-003 gizmos migration + Gizmo model

## Hard rules
- `branch_id` UUID FK branches (denormalized)
- Model is branch-scoped via lc_id correlation (NO global scope trait)
- DO NOT add new composer dependencies

## Implementation steps
1. Create migration `2026_06_01_120200_create_gizmos_table.php`:
   - `id` UUID PK
   - `name` string(120)
   - `branch_id` UUID FK branches
   - `created_at`, `updated_at`
2. Create model `app/Models/Gizmo.php`:
   - `HasUuids`
   - Branch isolation handled at query layer via lc_id (no trait)
   - Relationships: (none declared)

## Target files
```
database/migrations/2026_06_01_120200_create_gizmos_table.php
app/Models/Gizmo.php
tests/Unit/Models/GizmoTest.php
```

## Acceptance
- ✅ Migration runs cleanly
