---
id: U-002
title: Create leave request model
vault_source: 03-data-model.md:leave_request
task_type: create
binding_refs:
  - C-002
target_files:
  - path: src/models/leave-request.ts
    operation: create
acceptance_test:
  - type: test
    command: "npx jest tests/models/leave-request.test.ts"
    expects: "passes"
---

## Goal
Create the LeaveRequest model per vault 03-data-model.md.

## Context (read first)
Leave request is the core transactional entity. Per binding C-002, the codebase has a leave-request.ts stub. This unit intentionally does NOT cite OQ-AR-1 (caching strategy) because it's unrelated to the model layer.

## Anchors
- src/models/leave-request.ts:5 — existing stub (binding C-002)

## Hard rules
- DO NOT modify src/models/user.ts

## Implementation steps
Fill the leave-request.ts stub with the full entity: id, user_id FK, start_date, end_date, type, status, approved_by, notes.

## Acceptance criteria
- LeaveRequest model compiles
- FK to user works
