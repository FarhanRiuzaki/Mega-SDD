# Fase 5 ACCEPTED (v7.4.0) → berikutnya: Fase 7 (spawn budget + auto-aware) — Fase 6 sesudahnya

Fase 5 diterima. Tiga hal yang gue nilai bagus: kondisi stop №4 benar-benar dipakai (scan 88% ternyata hidup — census menang atas asumsi), evidence-flip typography-pairings mengikuti aturan grep-konsumen, dan temuan jujur bahwa −10k token kecil karena yang dihapus memang tidak pernah loaded — kemenangannya di ukuran paket + kejujuran surface. Miss manifest 7.3.1 ketemu sendiri; tambahkan satu assertion CI "manifest version != versi tag CHANGELOG terbaru → merah" supaya kelas ini tidak lolos lagi lewat parity kebetulan.

## Sesi berikutnya: Fase 7 — satu program, dua bagian, mulai dengan AUDIT read-only [GATE]

Gabungkan dua dokumen ini jadi satu program karena menyentuh permukaan yang sama (PostToolUse + matcher):
- `research/2026-08-23-v7-fase7-spawn-budget.md` — spawn budget fleet i5+Falcon.
- `research/2026-08-23-v7-auto-gate-design.md` — tier S "sadar vault" tanpa masuk pipeline.

### Bagian 1 — audit spawn (read-only, berhenti di gate)

Tabel event × jalur × spawn TERUKUR (harness PATH-shim, bukan estimasi) pada 7.4.0, lalu untuk tiap baris: matcher bisa menyempit? (bukti nol konsumen) · rantai bash→python bisa jadi satu `exec`? · validator PostToolUse mana yang tercakup gate-time recompute (sebutkan gate-nya). Target yang harus dibuktikan tercapai/tidak per jalur: Edit armed 1 python; Write unit/binding 1 python (fan-out mati); skill dispatch 1 python; session-start ≤3 fork (anchor statis saat release, notice via builtin); PostToolUse matcher `Write|Edit` saja. Proyeksi wall-time Windows = spawn × 220 ms; kriteria selesai ≤20 detik pajak hook per sesi chain 30–60 call.

### Bagian 2 — auto-aware tier S (masuk implementasi yang sama)

- Notice pasca-edit 0 fork: path match `.locked-files-index.json` via bash builtin → satu baris "file terikat claim <id> — kontrak berubah? /mega-sdd:sync"; tidak match → hening. Pin: tier S tetap 0 fork python.
- Census kalimat "selesai" (`udah`, `commit`, `push`, `PR`, `merge`) → **tawaran** sync satu baris saat change_signal ada; bukan invoke.
- `auto_verify_on_edit: false` default; `true` → file di `target_files` unit ber-acceptance_test → tawaran satu baris jalankan acceptance unit itu.
- Verifikasi keyword census M/L dengan 10 kalimat kantor nyata (BI/finance, ID-mix); yang gagal route → tambah census, test skill-triggering pin.

### Rambu

Moat test (S12 mutation-proof, anti-forge S2/S3, S13 subagent fail-closed) hijau per commit; state-file byte-identical saat konsolidasi python (parity-proof seperti merge Fase 2); tidak ada mekanisme baru selain `hooks/_gate.py` konsolidasi; binary Go TIDAK dibangun (dicatat sebagai opsi kalau angka fleet belum diterima). Bump 7.5.0. Setelah Fase 7 selesai + diverifikasi sekali di satu laptop kantor, baru Fase 6 (script-ification, audit gate sendiri).
