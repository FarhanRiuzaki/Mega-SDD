# God-review stage 5 — `generate-units` (the decomposer + unit-stage gates)

**Date:** 2026-07-02 · **Method:** adversarial multi-agent review — 8 blind lenses (schema-contract, task-typing-binding, unit-spec-validator, code-delivery-validators, dag-decomposition, prose-vs-enforcement, consumers-crosscut, hooks-dispatch) → dedup → refute-by-default verification (1 skeptic per finding; High requires an empirical repro). 46 agents, ~2.5M tokens (+1.4M on a session-limit-interrupted first verify pass, resumed from cache). Raw 48 → dedup 37 → **28 CONFIRMED + 4 DOWNGRADED, 5 REFUTED**.

**Severity after verification:** 10 High / 11 Medium / 11 Low.

**Shipped as v4.58.0** in four batches (5A gate-state integrity, 5B validate-unit-spec correctness, 5C flow-coverage & pack gates, 5D contract truth), pinned by `tests/god-review-s5/`.

## Dominant defect classes

1. **Gate state structurally incapable of holding the truth.** `.unit-spec-state.json` was a single slot overwritten per unit write — last-writer-wins meant only the LAST-written unit was ever gated (GU-HOOK-1, reproduced with a control: identical disk, opposite verdicts, decided by write order). Every quality state but the moat was agent-deletable/forgeable with absent=allow (GU-HOOK-2); Bash-written units bypassed all validators (GU-HOOK-3); async PostToolUse raced the sync gate (GU-HOOK-4); only the most-units vault was ever scanned (GU-HOOK-5).
2. **The validator judges a grammar the schema doesn't mandate (and vice versa).** Open enum on task_type (one casing character disarmed A1); doc/line-less anchors passed as grounding; the first target_files item swallowed by a regex; "closed" Hard-rule grammar with open catch-alls and silently-skipped evasion shapes; heading-presence-only contracts; an unchecked "No exceptions" acceptance_test rail; a citation checker that false-flagged its own documented template shape.
3. **A v4.57.0 protection defeated at its next consumer (again).** Resolved-KEEP_VAULT conflicts — the route stage 4 built — had NO task_type rule, so the code-update obligation was dischargeable by a no-code verify (GU-KEEPVAULT-1); the mandated SPLIT chain edge violated the strict-deps whitelist (GU-SPLIT-DEPS-4).
4. **Positive attestation without delivery.** risk:-keyed escalation with no producer; "REJECTED at render pass" with no check; "whitelist enforced at every step" with zero observers; phantom recovery flags; a mermaid mandate whose gate never read the mermaid.

## Verified findings


### GU-FC-FRONTMATTER [High] — flow-coverage (blocking gate) is blind to canonical frontmatter target_files — false FAIL blocks execute-bolts on schema-conformant units

**Where:** `plugins/mega-sdd/scripts/validate-flow-coverage.sh:558` · **Lens:** code-delivery-validators

**Failure:** Any brownfield project on a pack that declares endpoint_kinds (laravel/slim/symfony) whose units follow unit-schema.md exactly gets a permanent false FAIL: every input-accepting flow step counts as uncovered (artifacts_listed always 0), .flow-coverage-state.json goes FAIL, and PreToolUse hard-blocks every execute-bolts run. The remediation message instructs adding a section the canonical schema does not define — duplicating the whitelist into a non-schema body block is the only way to clear the block (verified: appending the body block flipped state to PASS).

**Evidence:** validate-flow-coverage.sh:546-572 (parse_unit: body-block-only extraction; line 553 frontmatter regex cannot read `- path:` items); unit-schema.md:83-88 (frontmatter whitelist) + 131-196 (Required body sections lack '## Target files'); generate-units/SKILL.md:80 (fills frontmatter whitelist); validate-unit-spec.sh:503-541 (_collect_target_files unions BOTH sources — the fix flow-coverage lacks); hooks/post-tool-use ~425-430 (unit-write dispatch runs flow-coverage); hooks/pre-tool-use:466-471 + 512-518 (status==FAIL → emit_block on execute-bolts); tests/fixtures/code-delivery/flow-coverage/{good,bad}/.../U-001.md use only the body-block format, zero fixtures carry frontmatter target_files. Repro (mktemp, laravel pack, canonical unit with 4 matching app/Http/Requests frontmatter entries vs 3 input-accepting flow steps): exit 1, {"status":"FAIL","missing_artifacts":[{"flow_steps_accepting_input":3,"artifacts_listed":0,"shortfall":3,"expected":"3 artifact(s) matching app/Http/Requests/**/*.php across unit(s) ['U-001']"}]}; after appending the same paths as a '## Target files' body …[truncated]

**Repro:** mktemp fixture: starterkit-context.yaml framework_pack: laravel; canonical 04-flows.md (### F-U-001: Widget Approval, Mermaid body, 3 DoD bullets with submit/approve wording); canonical unit U-001.md with frontmatter target_files listing 4 files under app/Http/Requests/. Ran validate-flow-coverage.sh --cwd=<proj>. Observed: exit 1, status FAIL, missing_artifacts=[{flow_steps_accepting_input: 3, artifacts_listed: 0, shortfall: 3, expected: '3 artifact(s) matching app/Http/Requests/**/*.php across unit(s) [U-001]'}] — despite 4 matching Requests in frontmatter. After appending the same list as a


### GU-FLOWCOV-1 [High] — flow-coverage derives 'input-accepting steps' from Definition-of-Done checkbox bullets, not the mandated Mermaid flow — the Mermaid branch is dead code on every canonical vault, producing both false-FAIL blocks and false-PASS on the gate's founding defect class

**Where:** `plugins/mega-sdd/scripts/validate-flow-coverage.sh:658` · **Lens:** dag-decomposition (corroborated: code-delivery-validators)

**Failure:** Direction 1 (false-FAIL): a unit shipping exactly the per-transition artifacts Step 2.2 requires is blocked — PostToolUse writes .flow-coverage-state.json status FAIL and PreToolUse Branch 5 (hooks/pre-tool-use:466-471) hard-blocks execute-bolts, with 'steps' that are literally DoD prose. Direction 2 (false-PASS): a flow whose input-accepting transitions live only in Mermaid edge labels while DoD bullets are signal-word-free yields n_input_steps=0, so a unit shipping ZERO Form Requests passes the exact gate built after the tradefinance '8 missing per-stage Form Requests' incident (script header lines 23-31) — under-decomposed units sail into bolts. The blocking gate is decoupled from the flow it claims to check on every canonical vault.

**Evidence:** Repro artifacts at /tmp/gu-flowcov-repro/{case1,case2,case3} (framework: laravel via .mega-sdd/codebase/codebase-map.md; vault .mega-sdd/vaults/demo/). Case 1 observed output: {"status":"FAIL", "missing_artifacts":[{"flow":"### F-U-001: Widget Approval","flow_steps_accepting_input":5,"artifacts_listed":3,"shortfall":2,"steps":["### F-U-001: Widget Approval step 1: - [ ] Maker can submit a widget draft", …5 DoD lines]}]} EXIT=1 — unit ships one Form Request per actual Mermaid transition yet blocked, with DoD checkboxes reported as 'steps'. Case 2 observed: {"status":"PASS","summary":{"flows_with_input_steps":0,…}} EXIT=0 — 5-transition maker-checker Mermaid flow + zero Form Requests passes. Case 3 (DoD removed): {"status":"FAIL","shortfall":5,"steps":["…step 1: S1[\"Maker submits widget draft\"] --> S2[\"Checker reviews widget\"]",…]} EXIT=1 — Mermaid branch functional but unreachable on canonical vaults. Root cause: validate-flow-coverage.sh:658 'elif any(TOPLEVEL_BULLET_RE.match(ln) for ln in lines)' fires on the col-0 '- [ ]' DoD bullets mandated by templates/04-flows.md:95-98 in …[truncated]

**Repro:** mktemp fixture: codebase-map.md (framework: laravel) + vaults/demo/04-flows.md '### F-U-001: Widget Approval' with 3-transition mermaid flowchart + 5 signal-word DoD bullets + U-001 listing 3 app/Http/Requests/*Request.php -> status FAIL, exit 1, shortfall 2, steps[]=['…step 1: - [ ] Maker can submit a widget draft', … 5 DoD lines]. Swapped fixture: 5 signal-laden mermaid edges ('Maker submits' -> 'Checker reviews' -> 'Approve or reject?' -> …), neutral DoD bullets, unit ships ONLY WidgetController.php (no Form Requests) -> status PASS, exit 0, flows_with_input_steps: 0.


### GU-HOOK-1 [High] — Render-test + verify-grounding (A1) execute-bolts gates are last-writer-wins: single-slot .unit-spec-state.json is overwritten on every unit write, so only the LAST-written unit is ever gated

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:585` · **Lens:** hooks-dispatch (corroborated: schema-contract, unit-spec-validator, prose-vs-enforcement, consumers-crosscut)

**Failure:** A verify+HIGH unit whose acceptance criteria certify UNBUILT behavior (the exact A1 false-green class), or a view-bearing unit with no render test, sails through the PreToolUse gate into execute-bolts whenever any clean sibling unit was saved after it — the normal case in every multi-unit generation, --regenerate, or --reconcile run. The false 'already built' verify then commits a green bolt over non-existent behavior (moat invariant #1's worst class). N-1 units in every N-unit run are effectively ungated; the gate outcome depends purely on write ORDER, not disk state.

**Evidence:** All cited evidence held exactly as claimed. Key anchors: plugins/mega-sdd/scripts/validate-unit-spec.sh:63 (STATE_FILE single slot), :583-600 (overwrite with one file's verdict); plugins/mega-sdd/hooks/post-tool-use:649-653 (per-write dispatch with --file-path) + hooks/hooks.json PostToolUse "async": true; plugins/mega-sdd/hooks/pre-tool-use:474-482 (gate reads the single slot as project-wide truth), :393-395 (unconditional gate-time re-derive exists ONLY for .validation-blockers.json). Empirical: with U-001 (verify+HIGH+[ungrounded]) and U-002 (clean) on disk, gate verdict flipped ALLOW/DENY purely by which unit's PostToolUse validation ran last — deny message observed: "mega-sdd:execute-bolts is blocked — unit-spec validator flagged 1 verify unit(s) stamped grounding_confidence: HIGH ... (verify_grounding_untrusted)".

**Repro:** mktemp project with .mega-sdd/vaults/demo/units/. U-001.md = verify + grounding_confidence: HIGH + '[ungrounded]' criterion; U-002.md = clean create. (1) Drive hooks/post-tool-use with Write of U-001: state -> {status: FAIL, halts: [verify_grounding_untrusted]}. (2) Drive it with Write of U-002 (last, as Step 10 would): state -> {checked_file: .../U-002.md, status: PASS, issues: []} — U-001's FAIL erased. (3) Drive hooks/pre-tool-use with Skill mega-sdd:execute-bolts: empty stdout, exit 0 = ALLOWED while U-001 still violates on disk. (4) CONTROL — re-drive post-tool-use with U-001 as last writ


### GU-HOOK-2 [High] — Every kept quality-gate state file except the moat file is agent-deletable (Bash rm allowed) and agent-forgeable (Write tool allowed); absent state = fail-open with no re-validation — a single command clears flow-coverage, render-test, verify-grounding, sibling-consistency, ui-quality and cross-cutting hard blocks

**Where:** `plugins/mega-sdd/hooks/pre-tool-use:790` · **Lens:** hooks-dispatch

**Failure:** execute-bolts is denied on any quality gate; the deny message prints the state file path; the agent runs `rm <root>/.mega-sdd/.unit-spec-state.json` (or Writes {"status":"PASS"} into .flow-coverage-state.json) — both tool calls are allowed — and the next execute-bolts sails through. The block is durable only until the next unit-glob Write, i.e. exactly until after the bolt already executed. The 'hard block' is one command away from cleared for every gate except binding->units.

**Evidence:** pre-tool-use:790 PROTECTED='\.validation-blockers\.json|\.plan-pending|\.replan-budget|\.iter-classifier\.json' — excludes all five quality state files. pre-tool-use:655 BC-BYPASS-1 protects only rel.endswith(".mega-sdd/.validation-blockers.json"). Aggregator L() (pre-tool-use:399-403) returns None on missing file; gates at 467-503 all key on `if d and d.get("status")=="FAIL"`, so None→no block. Only the moat is re-derived unconditionally (pre-tool-use:393-395 validate-handoff-binding-units.sh). Repro T2/C: rm of .unit-spec-state.json/.flow-coverage-state.json → exit 0 ALLOWED; T3/B: execute-bolts allowed after clear; T6/D contrast: moat rm+Write → DENY. Nuance (does not refute): the finding correctly excludes binding→units; PostToolUse re-derivation of these files fires only on unit/view/model writes, none of which occur during execute-bolts, so the cleared state persists across the bolt run.

**Repro:** Fixture with state at FAIL (verify_grounding_untrusted): pre-tool-use Skill(execute-bolts) -> deny JSON. Then: (a) pre-tool-use with Bash tool_input command 'rm $ROOT/.mega-sdd/.unit-spec-state.json' -> empty stdout, exit 0 = ALLOWED; after rm, pre-tool-use Skill(execute-bolts) -> stdout=[] exit=0 = ALLOWED. (b) pre-tool-use with Write tool_input file_path=$ROOT/.mega-sdd/.flow-coverage-state.json -> exit 0 = ALLOWED to forge; contrast Write of .validation-blockers.json -> deny (moat correctly protected).


### GU-HOOK-3 [High] — Unit files written/edited via the Bash tool never trigger the unit-stage validators (dispatch keyed exclusively to the Write|Edit tool path), so a stale PASS opens the quality gates over violating units

**Where:** `plugins/mega-sdd/hooks/post-tool-use:256` · **Lens:** hooks-dispatch

**Failure:** State reads PASS (last unit written via Write was clean). The agent then rewrites or adds a unit via `cat > <vault>/units/U-002.md <<EOF ... EOF` — e.g. while 'fixing' a unit after a halt, or generating units in a bash loop — introducing a verify+HIGH ungrounded spec (or a flow-coverage/sibling divergence). No validator re-runs; state stays PASS; execute-bolts is allowed over the violating unit. Reverse direction: a unit FIXED via sed leaves a stale FAIL false-block.

**Evidence:** Repro (mktemp fixture /tmp/gu3.5KW6pn, stale PASS .unit-spec-state.json for a clean U-001): (1) pre-tool-use with Bash tool_input `cat > <ROOT>/.mega-sdd/vaults/demo/units/U-002.md <<'EOF' ... task_type: verify / grounding_confidence: HIGH / criteria all [ungrounded] ... EOF` -> "exit=0" no deny (ALLOWED). (2) heredoc executed; violating U-002.md on disk. (3) post-tool-use fired with the same Bash payload -> exit=0, state unchanged: {"checked_file":".mega-sdd/vaults/demo/units/U-001.md","status":"PASS","issues_count":0,"issues":[]}. (4) pre-tool-use Skill(mega-sdd:execute-bolts) -> stdout=[] exit=0 (ALLOWED over the violating unit; verify-grounding/render-test/flow-coverage/sibling-consistency gates all read stale state, only the moat re-derives at pre-tool-use:393-395). (5) CONTROL: bash validate-unit-spec.sh --cwd=<ROOT> --file-path=<U-002> -> exit=1, state: status FAIL, halt_types ['verify_grounding_untrusted']. (6) CONTROL: same Skill(execute-bolts) call now DENIED: permissionDecision "deny" — "mega-sdd:execute-bolts is blocked — unit-spec validator flagged 1 verify unit(s) …[truncated]

**Repro:** Fixture with .unit-spec-state.json at PASS (clean U-002 written last via Write). (1) pre-tool-use with Bash tool_input command "cat > $ROOT/.mega-sdd/vaults/demo/units/U-002.md <<'EOF' ... verify+HIGH+[ungrounded] ... EOF" -> exit 0 ALLOWED. (2) Execute the heredoc; U-002 on disk now violates verify-grounding. (3) post-tool-use fired with the same Bash payload -> state unchanged: 'PASS .../U-002.md'. (4) pre-tool-use Skill(execute-bolts) -> stdout=[] exit=0 ALLOWED. Control: same U-002 content delivered via Write tool -> state FAIL -> deny.


### GU-HR-GRAMMAR-1 [High] — The 'closed 5-production' Hard-rule grammar is open in the validator — undocumented catch-all fallbacks accept ANY '- MUST/MUST NOT/DO NOT ...' line AND non-'- ' lines (asterisk bullets, numbered, bare prose) are silently skipped — so hard_rule_unparseable never fires for the dominant malformed classes

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:340` · **Lens:** schema-contract (corroborated: unit-spec-validator, prose-vs-enforcement)

**Failure:** generate-units emits a generic rule like '- MUST use soft deletes on all models' — the generate-side gate that exists to catch it (Step 12.5.b) passes it; at bolt time it either hard-halts the whole run with hard_rule_unparseable at the worst moment (strict v1 parse) or is silently never snapshot/validated: an unenforced constraint the unit claims is 'validated at bolt time' (unit-schema.md:149). Rules emitted with '*' bullets or prose phrasing are never grammar-checked at any layer and the documented halt never fires — a documented halt with no emitter for this input shape (invariant #4).

**Evidence:** All cited lines verified verbatim. Two refinements the lens missed, neither weakening the finding: (a) the non-'- ' skip at validate-unit-spec.sh:351-352 is partially load-bearing — it protects Citation:/Source: annotation sub-lines (required by checks 3-4 within 5 lines after a rule) and v2 ast-grep YAML block content from being flagged unparseable — so a fix must whitelist annotation/YAML shapes rather than flag all non-'- ' lines; (b) hard_rule_unparseable is not in CLAUDE.md's PreToolUse "actually enforced" list — the hooks/pre-tool-use aggregator (lines 474-482) lifts only render_test_missing and verify_grounding_untrusted from .unit-spec-state.json (grep: zero 'hard_rule' matches in pre-tool-use, post-tool-use, validate-preflight.sh) — so the defeated surface is the PostToolUse detection validator + lint-units "single source of truth" (lint-units.md:26-31), not a hard PreToolUse block. This strengthens rather than weakens the failure path: with the detection layer blind (fallbacks) or bypassed (non-'- ' shapes), the ONLY remaining coverage is bolt-side prose …[truncated]

**Repro:** Fixture U-001.md ## Hard rules: '- MUST use soft deletes on all models', '- DO NOT hardcode credentials anywhere' — matches no production -> status: PASS | issues: []. [C7b-bullet-evasion] section containing '* NEVER touch the payments ledger schema', bare 'DO NOT modify src/billing.py ...', '1. All handlers MUST be idempotent somehow' -> exit=0 status=PASS halts=[]. Control [C7-grammar-one-bad]: '- Always use dependency injection for services' -> FAIL ['hard_rule_unparseable', ...].


### GU-KEEPVAULT-1 [High] — Resolved-KEEP_VAULT conflicts have no task_type route in generate-units — the promised code-update obligation is dischargeable by a no-code verify unit (validator PASSes), or false-halts the documented resolve-oq→generate-units handoff

**Where:** `plugins/mega-sdd/skills/generate-units/references/task-typing.md:18` · **Lens:** task-typing-binding (corroborated: consumers-crosscut)

**Failure:** Vault says bearer-token auth (KEEP_VAULT-resolved CONFLICT-1); code has session auth. (a) generate-units passes the hard gate, finds no rule emitting an extend/create-toward-vault unit, and satisfies the propagation drop by listing CONFLICT-1 on a `verify` unit for an adjacent claim — validator PASS → execute-bolts runs the verify (no code change) → the promised code update silently never happens and the pipeline reports green (invariant #1's worst class + broken invariant-#3 obligation). (b) If the model aggregates by state, a KEEP_VAULT claim whose anchor resolves reads as IMPLEMENTED@high → verify → the bolt certifies the EXISTING, vault-DIVERGING code as correct — the exact inversion of 'implement toward the VAULT's claim'. (c) An agent applying 'Mix of CONFIRMED + CONFLICT → halt' literally deadlocks the documented KEEP_VAULT-only handoff.

**Evidence:** All cited lines verified as quoted, with one correction and one strengthening. Correction: failure path (b) is not reachable through documented surfaces — implementation-state.md:9 scopes the Implementation State Map to CONFIRMED claims, so a KEEP_VAULT claim never yields an IMPLEMENTED@high row for the model to aggregate or for lint-units.md:46 (which checks task_type against the State Map only) to endorse; drop (b) from the failure set. Strengthening: hard-rules-and-packs.md:28 ("KEEP_VAULT -> no Hard rule (intentional rewrite)") shows the ONLY binding->units bridge that could have carried the rewrite obligation deliberately emits nothing for KEEP_VAULT, so no artifact on any tier (rule, suggested hard rule, or validator) transports the code-update obligation into units. Repro independently reproduced: validate-handoff-binding-units.sh --cwd=<fixture> returned {"status": "PASS", "summary": {..., "conflict_ids_in_binding": 1, "conflict_ids_cited_by_some_unit": 1, "conflicts_unresolved": 0, "drops": 0}, "next_action": "No action needed — handoff trace is clean."} EXIT=0 against a …[truncated]

**Repro:** mktemp fixture: .mega-sdd/vaults/shop/binding.md with State Map row `C-002 CONFIRMED IMPLEMENTED ... high` + `### CONFLICT-1 RESOLVED (KEEP_VAULT — code update pending)` detail + resolution line; units/U-001.md with `task_type: verify`, `target_files: []`, `binding_refs: [C-002, CONFLICT-1]`, body 'No code changes...'. Ran validate-handoff-binding-units.sh --cwd=$T -> {"status": "PASS", "summary": {"conflict_ids_in_binding": 1, "conflict_ids_cited_by_some_unit": 1, "conflicts_unresolved": 0, "drops": 0}, "next_action": "No action needed — handoff trace is clean."} EXIT=0. The KEEP_VAULT code-u


### GU-TASKTYPE-ENUM-1 [High] — task_type/grounding_confidence frontmatter parsing is fragile — no closed-enum validation, no case normalization, no quoted-scalar support — so 'Verify', 'modify', or valid-YAML quoted values silently disarm the A1 verify-grounding gate and every per-type contract check

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:141` · **Lens:** schema-contract (corroborated: unit-spec-validator)

**Failure:** A hand-amended or model-drifted unit with `task_type: Verify` (one-char casing drift) or `task_type: "verify"` (one YAML style character) + grounding_confidence HIGH + [ungrounded] criteria (even with NO ## Anchors section) passes with status PASS and zero issues — no unit_underspecified, no verify_grounding_untrusted — so execute-bolts is never blocked and runs a false 'already built' verify (worst class per invariant #1). Invalid enum values (task_type: modify) likewise pass silently, skipping every per-type contract; extend's Migration-notes rail is disarmed the same way.

**Evidence:** validate-unit-spec.sh:141 `task_type_match = re.search(r"^task_type:\s*(\w+)", fm, re.MULTILINE)` — raw value, no enum/case normalization (contrast line 204 `.upper()` for grounding_confidence); lines 169/180/205 key on exact lowercase strings. Repro (re-run): U-001 control → FAIL ['unit_underspecified','verify_grounding_untrusted']; U-002 `task_type: Verify` → PASS []; U-003 `task_type: "verify"`+`grounding_confidence: "HIGH"` → PASS []; U-004 `task_type: modify` → PASS []; final .unit-spec-state.json = {"status":"PASS","issues_count":0} → pre-tool-use:474-482 (reads only this state file, counts verify_grounding_untrusted) leaves the execute-bolts A1 gate OPEN. No other validator in the post-tool-use unit dispatch (post-tool-use:404-452) checks task_type (grep: 0 matches in validate-handoff-binding-units.sh / validate-flow-coverage.sh / validate-sibling-consistency.sh); validate-bolt-artifacts.sh:310 lowercases but only gates B1 postflight; lint-units advisory; session-start:326 same regex, notices only. validate-flow-coverage.sh:555 strips quotes on the same frontmatter, proving …[truncated]

**Repro:** Fixture U-002.md (task_type: Verify, HIGH, no ## Anchors, criterion '[ungrounded] source-of-fund validation') -> status: PASS | issues: []. Byte-identical U-003.md with task_type: verify -> FAIL [unit_underspecified 'requires ## Anchors section', verify_grounding_untrusted]. U-006.md (task_type: modify) -> PASS []. [C3-quoted-yaml-values] task_type: "verify" + grounding_confidence: "HIGH" + '[ungrounded]' criterion + NO Anchors -> exit=0 status=PASS halts=[] -> GATE: OPEN; unquoted control (C1) -> FAIL ['verify_grounding_untrusted'].


### GU-VUS-A1-DOC-ANCHOR-1 [High] — A1 accepts the vault/PRD markdown itself (and line-less anchors) as 'non-test source' grounding — the exact criteria-live-only-in-the-PRD class the check was built to block passes

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:212` · **Lens:** unit-spec-validator

**Failure:** An agent instructed to add [grounded:] markers to a verify+HIGH unit for an UNBUILT behavior most plausibly cites the vault section where the claim lives (the only place the text exists). The validator passes it, the execute-bolts gate opens, and a false 'already built' verify unit certifies unbuilt behavior green — moat invariants #1/#5.

**Evidence:** Repro (mktemp fixture /tmp/gu-a1-refute.*, unit = task_type: verify + grounding_confidence: HIGH with ## Anchors + ## Acceptance criteria, run via `bash validate-unit-spec.sh --cwd=<fixture> --file-path=<unit>`): [C4] criterion `[grounded: .mega-sdd/vaults/demo/bound/03-entities.md:2]` -> exit=0 status=PASS halts=[]; [C5] `[grounded: src/billing.py]` (no line) -> exit=0 status=PASS halts=[]. Controls confirm the rail otherwise works: [C1] test-file anchor `[grounded: tests/test_billing.py:1]` -> exit=1 FAIL ['verify_grounding_untrusted']; [C6] out-of-range `[grounded: src/billing.py:9999]` -> exit=1 FAIL ['verify_grounding_untrusted']; [C7] real source anchor `[grounded: src/billing.py:1]` -> exit=0 PASS. Gate chain confirmed: hooks/pre-tool-use lines 474-482 read .mega-sdd/.unit-spec-state.json and block execute-bolts only when verify_grounding_untrusted issues are present — a PASS state opens the gate. Grep of scripts/ + hooks/ shows validate-unit-spec.sh is the sole deterministic parser of [grounded:] markers for unit acceptance criteria.

**Repro:** [C4-vault-doc-anchor] criterion '[grounded: .mega-sdd/vaults/demo/bound/03-entities.md:2]' -> exit=0 status=PASS halts=[]; [C5-lineless-anchor] '[grounded: src/billing.py]' (no line) -> exit=0 PASS. Controls prove the rail otherwise works: test-file anchor [C1] -> FAIL; out-of-range line [C6] '[grounded: src/billing.py:9999]' -> FAIL ['verify_grounding_untrusted'].


### GU-VUS-TF-SWALLOW-1 [High] — _collect_target_files swallows the FIRST frontmatter target_files item (\s* matches the newline), so render_test_missing never fires when the detail view is the first or only target file

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:509` · **Lens:** unit-spec-validator

**Failure:** Any unit whose detail/show view is the first (or sole) entry of the frontmatter target_files list — the normal shape for a view-bearing unit — yields targets missing that path, so the slice-D render gate is vacuously skipped and a detail view ships with no render acceptance test. Whether the enforced render-test gate fires depends on the ORDER of list items.

**Evidence:** validate-unit-spec.sh:509-522 — fm_tf = re.search(r"^target_files\s*:\s*(.*)$", frontmatter, re.MULTILINE): \s* after the colon matches the newline, so block-list form yields group(1)='- path: <first item>' (discarded — not '['), and after = frontmatter[fm_tf.end():] starts AFTER that line, dropping the first target file. Repro (real validator, real Laravel pack detail_view_glob at references/framework-conventions/laravel.md:317): U-001 (show.blade.php as sole target, no render acceptance_test) → {"status": "PASS", "issues_count": 0} exit=0; U-002 (identical but view listed second after a controller path) → {"status": "FAIL", "halt_type": "render_test_missing"} exit=1. Gate wiring: hooks/pre-tool-use:474-478 blocks execute-bolts only on render_test_missing issues present in .unit-spec-state.json; no other script emits it (repo grep: only validate-unit-spec.sh), so a view-first unit ships with the enforced render-test gate silently skipped. Fix shape: anchor the inline capture to non-newline whitespace (e.g. ^target_files[ \t]*:[ \t]*(.*)$) or scan items from the start of the line …[truncated]

**Repro:** Laravel pack active (codebase-map.md frontmatter framework: laravel; resolver returns detail_view_glob='resources/views/**/show.blade.php'). Unit with show.blade.php as ONLY target + no render test: [C8-detail-view-no-render] exit=0 status=PASS halts=[] (expected render_test_missing). Two-item control: view listed SECOND -> 'FAIL [render_test_missing]'; identical unit with view FIRST -> 'PASS []'. Debug print inside the real validator: DBG glob='resources/views/**/show.blade.php' seclen=2545 / DBG targets=[] matches=[] has_render=False.


### GU-HANDOFF-DRIFT-1 [Medium] — generate-units handoff blocks drift between handoff-contract.md and the operative auto-and-memory.md — halted-enum gaps (both omit unit_oq_trace_missing), metrics-key gaps, and a legacy `<vault>-bound/units` artifact example

**Where:** `plugins/mega-sdd/skills/orchestrate-flow/references/handoff-contract.md:397` · **Lens:** consumers-crosscut

**Failure:** An --auto chain that halts on unit_oq_trace_missing (or a module/interface halt) emits a blocker type outside both documented enums — orchestrate-flow operators and the handoff-consumption docs have no route for it; a producer following the auto-and-memory example emits a legacy -bound artifact path on a canonical-layout vault, which validate-handoff-yaml.sh's artifact-existence check (os.path.exists, artifact_missing halt at validate-handoff-yaml.sh:474) then fails, halting a successful run.

**Evidence:** Contract block (handoff-contract.md:370-397) vs operative emission (generate-units/references/auto-and-memory.md:36-79): (1) halted-on enums disagree — the contract (:397) omits interface_ref_missing, cross_squad_ambiguous, cross_module_dep_invalid, module_cycle_detected (all real Step 4/4.5/5 halts per halt-protocol.md:23-33 and SKILL.md:134), while auto-and-memory.md:77 omits memory_in_use; BOTH lists omit unit_oq_trace_missing — the Step 12.5.g MOAT-CRITICAL OQ-propagation halt (SKILL.md:111, halt-protocol.md:32,133-147) — and it is ALSO absent from orchestrate-flow/references/halt-taxonomy.md, so the moat rail's halt is absent from every documented handoff-status/routing enumeration. Correction: this does not leave operators route-less — convergence-loops.md:61 stops safely on any blocker.type not in CYCLE_ELIGIBLE and SKILL.md:94 surfaces blocker YAML verbatim — the defect is contract staleness, not misrouting. (2) The contract's metrics block (:383-385) lacks units_with_starterkit_anchors/units_with_starterkit_rules that auto-and-memory.md:55-56 requires in metrics (contract …[truncated]

**Repro:** Doc-contract drift; enums are not machine-validated (validate-handoff-yaml.sh checks structure/status/artifacts only, confirmed at lines 246/278/474).


### GU-HOOK-4 [Medium] — TOCTOU race: PostToolUse unit validators run async (hooks.json "async": true) while the sync PreToolUse gate reads their state files with re-validation only for the moat — execute-bolts dispatched right after the last unit/_index write can read absent or one-write-stale quality state

**Where:** `plugins/mega-sdd/hooks/hooks.json:34` · **Lens:** hooks-dispatch

**Failure:** Under --auto chaining (generate-units handoff -> execute-bolts within seconds), the Skill(execute-bolts) PreToolUse fires while the final unit/_index.md write's async validators are still running: .flow-coverage-state.json / .sibling-consistency-state.json are absent or reflect the previous write -> the gates pass vacuously; the first bolt dispatches before the FAIL lands. Not reproduced inside the real Claude Code scheduler (hooks driven manually), hence Medium.

**Evidence:** hooks.json:34 PostToolUse "async": true vs :22 PreToolUse "async": false. post-tool-use:409-450: six unit validators backgrounded then `wait` (comment: state files written "before this async hook exits" — no ordering vs the next tool call). pre-tool-use:393-395: only validate-handoff-binding-units.sh re-derived at gate time; :399-403 L() returns None on absent state → :467/474/485/492/500 quality gates raw-read with absent=allow. Empirical A/B (mktemp fixture, 1-unit vault): Skill(mega-sdd:execute-bolts) event with no state files → exit 0, no deny (gate-time moat state written PASS: {"status":"PASS","validator":"validate-handoff-binding-units.sh",...}); same event after printf '{"status":"FAIL","missing_artifacts":[{"shortfall":2}],"dead_scaffold":[...]}' > .mega-sdd/.flow-coverage-state.json → {"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"mega-sdd:execute-bolts is blocked — flow-coverage validator found 2 input-accepting flow step(s)..."}}. Timing: /usr/bin/time -p bash hooks/post-tool-use < write-event(U-001.md) → real …[truncated]

**Repro:** Timing only: `time (printf '{...Write U-002...}' | bash hooks/post-tool-use)` -> 1.546s total on a 3-unit fixture with every validator SKIP-ing early; hooks.json PostToolUse block carries "async": true while PreToolUse is "async": false.


### GU-MODFLAG-1 [Medium] — Documented halt recovery route points at flags generate-units does not declare (--derive-modules / --refresh-modules), and modules.yaml.auto acceptance is inconsistent across consumers

**Where:** `plugins/mega-sdd/skills/generate-units/references/modules-schema.md:234` · **Lens:** dag-decomposition

**Failure:** A user hits the `--module=` halt in execute-bolts, follows the documented recovery, and invokes a flag that no skill/command surface declares — the model either errors on an unknown flag or improvises; and because the sanctioned auto-derivation emits .auto (which execute-bolts' own prose refuses), the documented recovery route cannot actually clear the halt — a halt-taxonomy (invariant #4) recovery-route break.

**Evidence:** modules-schema.md:234-236 instructs '/mega-sdd:generate-units --derive-modules' and '--refresh-modules (NEW flag) — re-runs Step 5 module assignment without regenerating units'; execute-bolts/references/batch-and-fanout.md:30 makes --derive-modules the RECOVERY route for the '--module= requires _meta/modules.yaml' halt; commands/list-modules.md:22 repeats it. Neither flag appears in generate-units SKILL.md §Inputs & flags (SKILL.md:24-29) nor in commands/generate-units.md argument-hint. Additionally, Step 4.5 auto-derivation writes `_meta/modules.yaml.auto` (SKILL.md:74, modules-schema.md:85), which list-modules.sh accepts as a module source (list-modules.sh:228-231, verified on fixture) but execute-bolts' --module halt prose accepts only `_meta/modules.yaml` — so even a successful auto-derivation leaves the halt standing until the user knows to rename the file.


### GU-PACKLINT-GATES [Medium] — validate-pack.sh lints none of the gate-driving pack section headers — a typo'd '## Flow-artifact derivation' in a new pack silently downgrades blocking gates to SKIP

**Where:** `plugins/mega-sdd/scripts/validate-pack.sh:139` · **Lens:** code-delivery-validators

**Failure:** A contributor adds a new stack pack with a typo'd or renamed gate section header; validate-pack.sh passes it clean; flow-coverage and sibling-consistency — both PreToolUse blocking gates per CLAUDE.md — silently write SKIP forever on that stack, with no diagnostic distinguishing 'never declared' from 'declared but unmatchable'. A later SKIP overwrite also replaces any existing FAIL state on the next unit write, silently unblocking execute-bolts.

**Evidence:** validate-pack.sh (plugins/mega-sdd/scripts/validate-pack.sh) lints only 5 required-always sections (lines 139-144: File location standards / Naming standards / Idioms / Hard Rules emitted / Testing conventions) and 4 conditional sections (lines 153-157: Deep-scan file hints / Authz mapping / UI detection / Reuse discovery). None of the gate-driving sections ('## Flow-artifact derivation', '## Cross-cutting concerns', '## Relation derivation', '## UI quality signatures', '## Conditional scaffold artifacts', '## Entity source globs', '## Entity matching tokens', '## Test patterns') is checked, and _lint.md documents no such check. Mechanism correction: extract_section lives in scripts/_lib/resolve-framework-pack.sh (lines 213-238, case-insensitive PREFIX match; exit 3 only when the section is absent in EVERY pack of the chain, line 46). Because _template.md/scaffold-pack.sh lineage and all 26 shipped packs use 'extends: _universal', and _universal.md declares every gate header (Flow-artifact derivation line 118, Cross-cutting concerns 167, Relation derivation 190, etc.), a typo'd …[truncated]


### GU-PBT-REJECT-5 [Medium] — pbt-integration.md claims citation-less properties are 'REJECTED at generate-units render pass' but no render-pass check, SKILL step, or gate-read validator delivers it

**Where:** `plugins/mega-sdd/skills/generate-units/references/pbt-integration.md:200` · **Lens:** prose-vs-enforcement

**Failure:** A unit ships a `properties:` array with fabricated invariants carrying no cites field at all; every documented rejection point is a dead reference — nothing in the render pass, no validator, and no gate rejects it, so an uncited (potentially invented) invariant reaches the bolt as an executable obligation, violating the no-fabrication rail the same paragraph invokes.

**Evidence:** pbt-integration.md:200 "Properties without citations REJECTED at generate-units render pass" (plus :60 "properties without citations rejected" and :177 "High when citations enforced"); validation-passes.md:99-158 (12.5 checks a-g contain no properties check); SKILL.md:158 (sole properties mention, router only); validate-unit-spec.sh (0 matches for propert/cites/PROP); validate-bolt-artifacts.sh:548-579 (cite_pattern matches only D-NNN ADR shapes, 'if cited:' skips presence, misses documented vault-file cite shapes at pbt-integration.md:37-49); post-tool-use:637-639 writes .bolt-artifacts-state.json but pre-tool-use:436-506 L() reads exclude it; halt-protocol.md has no PBT-citation halt type.

**Repro:** Structural absence: validation-passes.md 12.5 shows checks a-g only (Anchors, grammar, prose, Migration notes, anti-patterns, starterkit citation, OQ trace); grep 'properties|cites' over generate-units SKILL.md procedure steps returns only router line 158; pre-tool-use L("...") reads = bolt-orphans, batch-suite-gate, bolt-postflight, flow-coverage, unit-spec, sibling-consistency, ui-quality, cross-cutting, factory-ledger — no bolt-artifacts.


### GU-RISK-FIELD-1 [Medium] — Adversarial-review subagent escalation is keyed to unit frontmatter `risk: high`/`risk: critical` — a field no producer writes and no schema defines

**Where:** `plugins/mega-sdd/skills/generate-units/SKILL.md:25` · **Lens:** consumers-crosscut

**Failure:** The D4-006 blind-spot mitigation's automatic escalation can never fire: auth/payment/high-risk units silently get the weaker same-thread QA re-prompt instead of the promised separate-subagent adversarial review (and the opus bump for critical units never happens) unless the user manually passes --adversarial-subagent. Prose promises an automatic behavior that structurally cannot occur.

**Evidence:** Consumers of a phantom `risk:` unit-frontmatter field: plugins/mega-sdd/skills/generate-units/SKILL.md:25 ("auto-set for any unit with `risk: high`"), SKILL.md:94 ("`--adversarial-subagent` (or `risk: high`) dispatches a separate subagent"), references/decomposition-rails.md:143 ("`--adversarial-subagent` flag OR unit `risk: high`"), references/adversarial-test-prompt.md:70 (dispatch SEPARATE subagent on `risk: high`) and :73 ("opus for `risk: critical` units"). Producers: none — grep of references/unit-schema.md returns zero matches for "risk" (Required frontmatter defines id/title/vault_source/task_type/status/grounding_confidence/grounding_evidence/mutability, no risk key); references/templates/unit.md has no risk field; plugin-wide grep for "risk:" (excluding .mega-sdd artifacts) hits only the four consumer files plus unrelated prose at skills/scan-codebase/references/scan-procedure.md:103; scripts/validate-unit-spec.sh only enforces required-field presence (unit_underspecified) and never emits or checks risk. execute-bolts' review-panel.md "Risk signals" (5 computed signals …[truncated]

**Repro:** Structural absence (grep evidence above); no runtime component exists to execute.


### GU-SK-CITE-2 [Medium] — starterkit_rule_citation_missing 'machine-checked / ALWAYS STOP' backstop misses its own documented rule templates and false-positives on the documented citation shape

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:382` · **Lens:** prose-vs-enforcement

**Failure:** False negative: an uncited starterkit-derived rule built from the reference's own SweetAlert2 template passes with no starterkit halt — an invented/uncited convention rule ships in the unit (citation-discipline invariant #3) while docs promise ALWAYS STOP. False positive: a rule cited exactly per the derivation template is flagged missing-citation, polluting state/telemetry.

**Evidence:** Repro (mktemp vault, canonical .mega-sdd/vaults/demo/units/ layout): (1) U-003.md — frontmatter starterkit_context_consumed: true; Hard rule "- MUST use SweetAlert2 for confirmations and notifications (NEVER native alert() or window.confirm())" (verbatim from starterkit-derivation.md:104), no citation → observed: status FAIL, issues = [hard_rule_trace_missing sev:advisory] only; NO starterkit_rule_citation_missing (false negative vs SKILL.md:86 + starterkit-derivation.md:185 "machine-checked... ALWAYS STOP"). (2) U-006.md — "- MUST follow starterkit idiom: use Flux components" + next line `citation: "starterkit-context.yaml §ui_ux.idioms"` (exact documented template shape, starterkit-derivation.md:91-93) → observed: starterkit_rule_citation_missing flagged (false positive). (3) Control U-007.md — same rule + `Citation: starterkit-context.yaml §ui_ux.idioms` (SKILL.md:86 capitalized shape) → PASS, proving the two documented citation shapes conflict and only one satisfies validate-unit-spec.sh:386. (4) Control U-005.md — "starterkit" in text, uncited → caught, proving the substring …[truncated]

**Repro:** U-003.md: starterkit_context_consumed: true, Hard rule '- MUST use SweetAlert2 for confirmations and notifications (NEVER native alert() or window.confirm())', no citation -> FAIL [('hard_rule_trace_missing','advisory')] — NO starterkit_rule_citation_missing. U-006.md: '- MUST follow starterkit idiom: use Flux components' + next line 'citation: "starterkit-context.yaml §ui_ux.idioms"' (the documented template shape) -> FAIL [('starterkit_rule_citation_missing','HARD')] despite being correctly cited. Control U-005 ('starterkit' in text, no citation) is caught, proving substring gating.


### GU-SPLIT-DEPS-4 [Medium] — The mandated SPLIT chain edge (create depends_on verify) matches none of the five strict-deps evidence classes — Step 2.5 orders an edge Step 4 forbids

**Where:** `plugins/mega-sdd/skills/generate-units/references/decomposition-rails.md:28` · **Lens:** task-typing-binding

**Failure:** A candidate aggregating NEW + IMPLEMENTED@high claims splits into U-010(verify) + U-011(create). Under the default strict-deps rules the chain edge is prohibited, so a rule-following agent drops it; execute-bolts topologically sorts and runs both in parallel (unit-schema.md §Dependency graph) — the create bolt lands new code before/while the verify assertions run against the pre-change implementation, breaking the documented verify-first sequencing; an agent keeping the edge violates the emission whitelist. Same-claim ordering becomes run-dependent.

**Evidence:** Mandate: SKILL.md:59 'Mix of NEW + IMPLEMENTED → SPLIT (one create, one verify; chain so verify runs first)'; task-typing.md:26 'chain via depends_on so verify runs first'. Prohibition: decomposition-rails.md:28 'Emit depends_on: U-X ONLY IF' classes a-e (28-37) — none matches: verify target_files 'empty OR all entries operation: none' (task-typing.md:44, unit-schema.md:248) are existing files, create half targets new files (Step 7.6 collision converts existing-file creates to extend, task-typing.md:87-101) → class (a) empty intersection; verify plans no symbols → (b) fails; ordering source is binding State Map not vault → (d) fails; same candidate/module → (e) fails. decomposition-rails.md:39-42 DO-NOT list names the shape: 'Same vault section / same module' + 'Conceptual sequencing without file overlap'. Prohibition restated at SKILL.md:67 and SKILL.md:123. No reconciliation: grep for 'verify runs first|verify-first' and 'strict-deps' across plugins/mega-sdd/ returns only the cited lines. No deterministic backstop: validate-unit-spec.sh has zero depends_on logic; lint-units.md:42 …[truncated]


### GU-TTCONTRACT-1 [Medium] — Per-task_type contract checks are heading-presence only: empty ## Anchors and empty ## Migration notes pass, and a create unit WITH Migration notes passes (12.5.d MUST-NOT direction unchecked)

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:170` · **Lens:** schema-contract

**Failure:** An extend unit with bare '## Anchors' + '## Migration notes' headings and zero content passes (bolt gets no anchor to read and no ADD/KEEP/REMOVE field plan — the exact PARTIAL_FIELDS_* payload the binding field_diff propagation exists to deliver); a create unit carrying a stray Migration-notes section (task_type mis-assignment signal per the schema) also passes.

**Evidence:** validate-unit-spec.sh:169-177 (Anchors: `re.search(r"^##\s+Anchors?", ...)` heading-only) and :180-188 (Migration notes: `re.search(r"^##\s+Migration\s+notes?", ...)` heading-only); no create/verify MUST-NOT check exists anywhere in the script. Contract: validation-passes.md:103-106 (12.5.a '>=1 entry'), :115-117 (12.5.d 'all three sub-lists (REMOVE / KEEP / ADD) ... must be present' / 'MUST be absent' for create|verify); unit-schema.md:173, :244-248. Repro (observed, /tmp/gu-ttcontract.tCLq06): U-004.md (task_type: extend, empty '## Anchors' + empty '## Migration notes') -> {"status": "PASS", "issues_count": 0} exit 0; U-005.md (task_type: create, populated '## Migration notes') -> {"status": "PASS", "issues_count": 0} exit 0. Impact scope: unit_underspecified never blocks execute-bolts (hooks/pre-tool-use:474-482 reads .unit-spec-state.json only for render_test_missing + verify_grounding_untrusted; validator header line 16 'Detection-only at hook layer'), and commands/lint-units.md:26 treats this validator as 'the single source of truth for these checks', so a degenerate extend …[truncated]

**Repro:** Fixture U-004.md (task_type: extend, '## Anchors' and '## Migration notes' headings both empty) -> status: PASS | issues: []. Fixture U-005.md (task_type: create WITH a populated ## Migration notes section) -> status: PASS | issues: [].


### GU-VUS-AT-PRESENCE-1 [Medium] — acceptance_test presence is not checked by any deterministic validator despite the schema's 'No exceptions' rail and the validator's own header claiming acceptance_test detection

**Where:** `plugins/mega-sdd/scripts/validate-unit-spec.sh:11` · **Lens:** unit-spec-validator (corroborated: schema-contract, prose-vs-enforcement)

**Failure:** A unit with NO acceptance_test key at all (or an empty list, or only type: manual entries) passes unit-spec validation with status PASS and reaches execute-bolts with nothing asserting the bolt's success criteria — the anti-hallucination rail is prose-only while the validator's header claims coverage; a drifting bolt run then implements with no test obligation while the pipeline state reports the unit spec-clean.

**Evidence:** Repro (2026-07-02, mktemp fixture, macOS bash 3.2): unit with all required frontmatter but no acceptance_test key -> validate-unit-spec.sh --cwd=$TMP --file-path=$TMP/.mega-sdd/vaults/demo/units/U-001.md => {"status": "PASS", "issues_count": 0, "issues": []} EXIT=0; acceptance_test: [] variant (U-002.md) => same PASS EXIT=0. Code: required-field list validate-unit-spec.sh:128 + :145-157 omits acceptance_test; only render-type check exists (:544-579, pack-glob-gated). Header :10-12 claims acceptance_test-missing detection scope. Coverage sweep: pre-tool-use consumes only render_test_missing (:476) + verify_grounding_untrusted (:480) from .unit-spec-state.json; validate-preflight.sh has no acceptance_test check (input-presence only); post-tool-use: no hits; scripts/ grep: only unit-spec render + fanout-parity render mirror; lint-units.md:2/:6/:74 advisory read-only, delegating to the same validator (:10,:23); execute-bolts/SKILL.md pre-flight check 2 acceptance_test prose is unhooked. Rail prose: unit-schema.md:311 ('No exceptions', with adjacent render bullet explicitly hook-wired), …[truncated]

**Repro:** [C9-no-acceptance-test] unit with all required fields but no acceptance_test key -> exit=0 status=PASS halts=[]. Also confirmed on fixture U-004.md: only an unrelated [('hard_rule_trace_missing','advisory')] emitted; no unit_underspecified/acceptance_test issue.


### GU-WHITELIST-6 [Medium] — target_files whitelist 'enforced at bolt time / at every step' has zero deterministic observer — delivery is prompt prose only, with no post-hoc violation detection

**Where:** `plugins/mega-sdd/skills/generate-units/SKILL.md:119` · **Lens:** prose-vs-enforcement

**Failure:** A bolt subagent that drifts and edits a file outside the unit's whitelist commits successfully; no gate, postflight check, or batch scan can even observe the violation after the fact — yet operators reading 'enforced at every step' assume a deterministic rail equivalent to the render-test/sibling gates. Per the enforcement doctrine a prose gate is legitimate design, but four surfaces claiming 'enforced' with no detectability seam (unlike Hard rules, which earned the B1 artifact gate precisely because prose-only scanning failed) is positive attestation without delivery.

**Evidence:** Verified surfaces claiming enforcement: plugins/mega-sdd/skills/generate-units/SKILL.md:119 ("no unit may touch files outside `target_files` (enforced at bolt time)"); skills/generate-units/references/unit-schema.md:257 ("`target_files` whitelist is enforced by `execute-bolts`"); skills/execute-bolts/SKILL.md:122 ("`target_files` whitelist enforced at every step"); skills/generate-units/references/pagerank-targeting.md §Output integration ("only `target_files` (frontmatter whitelist) is enforced"); plus the execute-bolts command/skill description ("target-files whitelist enforced"). Verified delivery gap: superpowers-bridge.md:89-93 §Whitelist enforcement is controller prose; bolt-dispatch-prompt.md:149 ("DO NOT touch outside") + :152 (self-halt scope_creep_detected) is subagent prose — scope_creep_detected has zero emitters in scripts/ or hooks/; hooks/pre-tool-use + post-tool-use contain zero target_files/whitelist branches; validate-bolt-artifacts.sh Check 1 (provenance_missing) only fires when the written file IS in a unit's target_files (find_unit_for_target → (None,None) for …[truncated]

**Repro:** Structural absence: grep -rn 'target_files|whitelist' over scripts/ and hooks/pre-tool-use shows no violation-detection code path; validate-bolt-artifacts.sh contains no touched-files-vs-whitelist comparison.


### GU-ATOMID-1 [Low] — Split-unit ID grammar conflict (U-001.1/U-001.2 vs topological U-00N) and the <300 LOC / ≤5 files atomization threshold is measured by nothing

**Where:** `plugins/mega-sdd/skills/generate-units/references/unit-schema.md:256` · **Lens:** dag-decomposition

**Failure:** The model follows §Atomicity and emits U-014.1/U-014.2, breaking the topological-numbering + content-hash ID-stability contract that --refresh/--reconcile and _index.md regeneration depend on (two units whose IDs no other doc's grammar anticipates); separately a 12-file 'atomic' unit ships with no signal anywhere — the PR-sized-bolt invariant is vibes-only at every deterministic surface.

**Evidence:** unit-schema.md:256 ("SPLIT into U-001, U-001.1, U-001.2") is the ONLY occurrence of a dotted sub-ID grammar in the entire plugin and conflicts with: unit-schema.md:24 ("id: U-001 # zero-padded, monotonic"), SKILL.md:65 Step 3 ("larger → split into N units with an explicit depends_on chain"), SKILL.md:78 Step 6 ("Topologically sort candidates; number U-001, U-002, …; default re-run preserves IDs of unchanged units by content hash"), and decomposition-rails.md:88-94 §ID allocation. No deterministic surface rejects or anticipates dotted IDs — post-tool-use:406, validate-unit-spec.sh:58-59, list-modules.sh:268-269, and compute-unit-staleness.sh:66 all use permissive U-* globs — so the impact is model-emitted grammar drift against the prose ID-stability contract that SKILL.md:28 (--reconcile: "ID-stability contract holds — never duplicates") depends on, not tool breakage. Secondary claim narrowed: the >300 LOC / ≤5 files threshold is indeed unmeasured (validate-unit-spec.sh:155-157 checks target_files presence only; lint-units.md:52 reports "average target_files/unit" as a statistic; no …[truncated]


### GU-FORCECREATE-DEDUP-7 [Low] — Step 7.6 force-create is documented as soft-proceed but Step 12.6 unconditionally halts the same condition, with a recovery flag marked NOT YET IMPLEMENTED — and 12.6 keys on possibly-truncated codebase-map §1 instead of the fs

**Where:** `plugins/mega-sdd/skills/generate-units/references/validation-passes.md:162` · **Lens:** task-typing-binding

**Failure:** User in a rebuild scenario picks option 4 (force create) at Step 7.6 for a unit whose 2 target files exist; generation proceeds per the soft-proceed matrix, then Step 12.6 hard-halts dedup_ambiguous on the same files, pointing at a flag that does not exist; conversely on a truncated map the identical mistaken-create unit sails through 12.6 because §1 never listed the files.

**Evidence:** Two precision fixes to the lens's evidence, neither changing the verdict: (1) codebase-map §1's per-file incompleteness stems from depth-limiting by design ('[ tree of dirs, depth-limited ]', codebase-map-schema.md:35-36; scan-procedure.md Step 4, default --depth=8), not primarily the 200-per-category truncation cap — the truncated_sections marker's cap semantics are exemplified on §2 symbol categories (codebase-map-schema.md:27-30). The blindness claim holds either way, arguably more strongly (depth-limited dirs may omit file entries even on small repos). (2) The recovery is not a pure infinite loop: halt-protocol.md:111 honestly labels the flag 'NOT YET IMPLEMENTED — pause and consult human', so it is a documented dead-end contradiction — the matrix promises proceed-on-accepted-risk (defensive-generation.md:181, halt-protocol.md:170) while validation-passes.md:162-166 as written re-halts the identical user-accepted unit with no reconciliation rule and points at a phantom flag (sole occurrence in the plugin: halt-protocol.md:111; absent from SKILL.md:24-29 flags).


### GU-GCONF-1 [Low] — defensive-generation.md contradicts itself and the live gate on grounding_confidence semantics: 'descriptive … doesn't gate downstream' vs the A1 verify+HIGH block 32 lines earlier

**Where:** `plugins/mega-sdd/skills/generate-units/references/defensive-generation.md:188` · **Lens:** schema-contract

**Failure:** An agent loading §Anti-halu rails as the operative summary stamps grounding_confidence: HIGH freely on verify units ('it's just metadata'), then trips the A1 gate — or worse, argues from this line that the gate state is safe to clear. Stale pre-A1 prose inside the same reference that defines A1.

**Evidence:** defensive-generation.md:188 ("`grounding_confidence` is descriptive (frontmatter metadata), not prescriptive (doesn't gate downstream)") vs defensive-generation.md:156 (verify+HIGH ungrounded criterion "is blocked by `validate-unit-spec.sh` → `verify_grounding_untrusted` (the next `execute-bolts` halts)"). Enforcement confirmed at validate-unit-spec.sh:204-206 (`if task_type == "verify" and grounding_conf == "HIGH":` — the field value IS the gate trigger), hooks/pre-tool-use:479-482 (aggregator fails execute-bolts on verify_grounding_untrusted), unit-schema.md:46 ("Enforced: validate-unit-spec.sh halt verify_grounding_untrusted (HIGH verify units only)"). Note the "descriptive; not enforced downstream" phrasing correctly belongs to the sibling field grounding_evidence (unit-schema.md:50) — line 188 misapplies it to grounding_confidence. Fix: reword line 188 to scope it (descriptive for create/extend and MEDIUM/LOW; prescriptive for verify+HIGH per A1).

**Repro:** Textual contradiction; the gating side was empirically confirmed in the last-writer-wins repro (verify+HIGH -> verify_grounding_untrusted FAIL).


### GU-GRAPH-CONFLICT-1 [Low] — build-graph.sh types every non-OQ binding_ref as 'claim' — the CONFLICT-NNN refs the moat REQUIRES units to carry become permanently-pending mislabeled nodes; claim nodes also omit the new state_reason/resolution fields

**Where:** `plugins/mega-sdd/scripts/build-graph.sh:288` · **Lens:** consumers-crosscut

**Failure:** The impact lens (/mega-sdd:graph) shows phantom pending 'claim' nodes for exactly the conflict-traceability links the moat mandates, and cannot distinguish a resolved-KEEP_VAULT conflict from a live one — an operator triaging via the graph sees a resolved binding as still-conflicted. Advisory surface only (graph is derived/diagnostic), hence Low.

**Evidence:** build-graph.sh:288 `pending(tid, "oq" if ref.startswith("OQ") else "claim", relp(upath), "binding_refs")`; claim nodes keyed `f"{vid}:{c['id']}"` with attrs only {verdict, state} (build-graph.sh:221-225). Mandate for CONFLICT-NNN in binding_refs: generate-units/SKILL.md:39, resolve-oq/references/binding-mode.md:43, validate-handoff-binding-units.sh conflict_id_dropped (~line 243). Empirical repro output from crafted fixture: node `demo:CONFLICT-1` type=claim attrs={"pending": true} with edge `demo:U-001 -honors-> demo:CONFLICT-1`; node `demo:C-001` attrs={"verdict": "CONFLICT", "state": "IMPLEMENTED"} despite binding.json carrying resolution=KEEP_VAULT and state_reason=truncated_section. Graph spec docs/superpowers/specs/2026-06-24-graph-layer-design.md defines `honors | unit → claim / oq` (binding_refs described as "unit→C/OQ") — the CONFLICT-NNN namespace is unhandled by design omission, not covered elsewhere.

**Repro:** Static code-path evidence; graph is a non-gating diagnostic so no gate-level repro applies.


### GU-HALT-TAXO-1 [Low] — Halt-taxonomy gaps: hard halt target_files_collision is documented but has NO emitter anywhere; the three validator-emitted halt types are absent from halt-protocol.md despite the 'Full blocker YAML for every type' routing claim

**Where:** `plugins/mega-sdd/skills/generate-units/references/defensive-generation.md:180` · **Lens:** schema-contract

**Failure:** Invariant #4 (every documented halt has a real emitter + recovery route): an agent hitting the all-targets-collide / no-path-to-resolve state finds a documented hard halt that nothing can emit and no recovery YAML (--auto behavior undefined); an agent hitting render_test_missing / verify_grounding_untrusted and following SKILL.md:131 to halt-protocol.md finds no entry for the blocker it must recover from.

**Evidence:** defensive-generation.md:180 names a hard halt `target_files_collision` that no step emits and no taxonomy defines (grep: only defensive-generation.md:131,180 + unit-schema.md:53, the latter two being the unrelated *_check frontmatter field; absent from halt-protocol.md and the vault-contract.md:696 enum). The condition it describes is already emitted under the canonical name `dedup_ambiguous` (SKILL.md:112 Step 12.6; halt-protocol.md:29,97-113 with full recovery YAML; vault-contract.md:790 registry entry), and halt-protocol.md:170's own summary lists the collision force-create path as SOFT and dedup_ambiguous as the hard halt — so the matrix row at defensive-generation.md:180 is a stale alias. Since halt-protocol.md:170 routes readers to this matrix as 'the full halt-vs-warning matrix', an agent could emit the non-canonical type name under --auto, which vault-contract.md:804 says schema validation would reject as invalid_handoff. Fix: rename the row's halt name to `dedup_ambiguous` (or delete the stale row). The three validator-emitted types (render_test_missing, …[truncated]

**Repro:** Structural: `grep -rn target_files_collision plugins/mega-sdd/` returns only defensive-generation.md:131,180 and unit-schema.md:53; grep of render_test_missing|verify_grounding_untrusted|hard_rule_trace_missing over halt-protocol.md and vault-contract.md returns nothing.


### GU-HOOK-5 [Low] — flow-coverage and sibling-consistency gate only the single vault with the MOST units — violations in any other active vault are invisible to the execute-bolts block

**Where:** `plugins/mega-sdd/scripts/validate-flow-coverage.sh:346` · **Lens:** hooks-dispatch

**Failure:** A phased KB rebuild (generate-intent --phase=N) or any project with two vaults under .mega-sdd/vaults/: vault A has 30 units (clean), vault B has 8 units including a flow step with no mapped input-validation artifact. Every dispatch validates only vault A; .flow-coverage-state.json stays PASS; execute-bolts against vault B's units is never blocked on B's flow-coverage violations. Deliberate 'active phase' heuristic, but it silently un-gates the smaller vault rather than SKIP-ing loudly.

**Evidence:** Repro (macOS bash 3.2, mktemp dir): cp -R tests/fixtures/code-delivery/flow-coverage/bad/. $T/ ; run validate-flow-coverage.sh --cwd=$T -> "BASELINE status: FAIL | missing: 1 | vault: demo-phase". Then add a second clean vault $T/.mega-sdd/vaults/phase-one/ (good fixture's 04-flows.md + 3 units U-001..U-003) and re-run -> "TWO-VAULT status: PASS | missing: 0 | selected vault: phase-one"; $T/.mega-sdd/.flow-coverage-state.json (the file pre-tool-use:467 reads) now shows status: PASS, vault: phase-one — demo-phase's missing input-validation artifact is invisible to the execute-bolts gate. Code anchors: validate-flow-coverage.sh:345-346 (most-units max()), validate-sibling-consistency.sh:186-204 esp. :199 (same max()), pre-tool-use:466-471 + 484-489 (status-only read, no vault keying), post-tool-use:425-436 (validators dispatched with --cwd=$PROJECT_ROOT on any unit write, including writes into the smaller vault).


### GU-PROBE-ANCHOR-3 [Low] — Probe-minted verify units contradict the 'verify with empty binding anchor → halt (binding gap)' rule, and the probe result is recorded nowhere

**Where:** `plugins/mega-sdd/skills/generate-units/SKILL.md:63` · **Lens:** task-typing-binding

**Failure:** On a truncated-map brownfield run, an agent privileging SKILL.md halts every probe-derived verify as 'binding gap' (recovery says re-run bind-codebase, which re-produces the same truncation → loop), while an agent privileging task-typing.md ships verify units whose only evidence lives in an unrecorded grep, untraceable in binding.json/State Map for any later audit, reconcile, or lint 'task_type vs binding state' check.

**Evidence:** Two doc-consistency gaps remain from this finding. (a) plugins/mega-sdd/skills/generate-units/SKILL.md:121 — '`verify` units MUST have a concrete binding `anchor` (missing → downgrade-to-create only if no anchor exists; a `verify` with intent but no anchor halts as a binding gap)' is a v4.0.0-era compression (commit 5e769a5) of the retired pre-v4 rule '(v1.2+, Iter 1) verify units MUST have a concrete anchor from binding; missing anchor → downgrade to create'; the 'downgrade-to-create' fragment textually contradicts SKILL.md:63's 'never silently downgrade' in the same file — reword the parenthetical to point at the task-typing.md UNKNOWN sub-rule (probe not-found → create at typing time; assigned verify without anchor → halt). (b) plugins/mega-sdd/skills/generate-units/references/halt-protocol.md — the 'verify-without-anchor binding-gap halt' is listed among hard halts at line 168 but has no YAML block and no row in the 'When each halt fires' index (lines 21-33), while SKILL.md:131 promises 'Full blocker YAML for every type'; add an index row + YAML block (fires in Step 2.5; …[truncated]


### GU-RECONCILE-MATCH-6 [Low] — Reconcile's claim↔unit match key (vault_source) is structurally ambiguous after any SPLIT or atomization, and its ambiguity halt reuses dedup_ambiguous whose blocker YAML/recovery describes a different condition

**Where:** `plugins/mega-sdd/skills/generate-units/references/task-typing.md:113` · **Lens:** task-typing-binding

**Failure:** Run `generate-units --reconcile` on a vault containing one SPLIT pair (U-010 verify + U-011 create, same vault_source): the match is ambiguous per the rail → dedup_ambiguous halt whose emitted YAML and next_action instruct the user to rename target_files of a unit that has none — an un-followable recovery; the living-vault sync lane deadlocks on the pipeline's own normal decomposition output, or the agent silently guesses which unit to flip (the rail's forbidden outcome).

**Evidence:** Doc drift between the local and canonical definitions of dedup_ambiguous: plugins/mega-sdd/skills/generate-units/references/halt-protocol.md:29 ("Step 12.6 | a `create` unit's `target_files` ALL already exist") and :97-113 (YAML with conflicting_paths + rename-target_files resolutions) document ONLY the Step-12.6 trigger, while task-typing.md:124 reuses the halt type for reconcile claim-unit match ambiguity, whose matching trigger definition and recovery ("dedupe step finds multiple existing units that could match a new claim ... Resolution: user picks the canonical unit OR confirms creating a new one") exist only in the canonical registry at generate-intent/references/vault-contract.md:790 (which halt-protocol.md:17 declares conformance to). An agent loading only halt-protocol.md to emit the reconcile-ambiguity halt finds a YAML template whose details/resolutions fit the other trigger. Fix is doc-completeness: add the second trigger variant (or an explicit pointer to vault-contract.md:790's shape) to halt-protocol.md's index + YAML section, and optionally name binding_refs …[truncated]


### GU-SKC-INDENT [Low] — starterkit-conformance patterns parser is indentation-locked and parses the documented §patterns schema shape to zero patterns — silent SKIP disables every conformance check

**Where:** `plugins/mega-sdd/scripts/validate-starterkit-conformance.sh:88` · **Lens:** code-delivery-validators

**Failure:** scan-codebase's Step 10.5.3 consolidator (an AI writer following starterkit-context-schema.md) or any standard YAML re-serialization emitting the §patterns example shape produces a file where all v4.56.0 5-slice pattern conformance checks (controller/data_model/request_validator/test locations) silently never run — violations like a controller outside the starterkit location are invisible, reported as SKIP (exit 0) instead of FAIL.

**Evidence:** Verified: validate-starterkit-conformance.sh:83 requires categories at exactly 4 leading spaces (re.match(r"^    (\w+):\s*$")) and line 90 requires fields at exactly 6 (re.match(r"^\s{6}(\w+):\s*(.+)")). Repro in mktemp project: (a) canonical shape per starterkit-context-schema.md:27-102 / deep-scan-stage.md:281-354 (starterkit_context: root, patterns at 2, categories at 4, fields at 6) + 3 violating target_files -> {"status":"FAIL","violations_count":3,"patterns_loaded":["controller","data_model","test"]}, exit 1 — parser handles the documented file shape; (b) same content with patterns: at column 0 (the §examples excerpt indentation, and equivalently any valid-YAML variant like 4-space-per-level nesting) -> {"status":"SKIP","detail":"starterkit-context.yaml has no patterns: block"}, exit 0 — fail-open on writer indentation drift. Consumers verified: .starterkit-conformance-state.json read only by scripts/run-analyze.sh (lines 225, 394); hooks/pre-tool-use contains zero starterkit-conformance references; hooks/post-tool-use:417/541 invoke the validator with "|| true" purely to …[truncated]

**Repro:** mktemp fixture: starterkit-context.yaml copied from schema-doc Example A shape (patterns: col 0, categories at 2 spaces, fields at 4) + unit with 3 deliberately violating target_files -> {"status": "SKIP", "detail": "starterkit-context.yaml has no patterns: block"}, exit 0. Re-indented the SAME content (+2 spaces per level): {"status": "FAIL", "violations_count": 3, patterns_loaded: [controller, data_model, test]}, exit 1 — indentation alone flips detection of 3 real violations to silence.


### GU-SQUADREF-1 [Low] — generate-units references route Step 5 to bare `squad-partition.md`, which does not exist in generate-units/references/ — the routing/validation detail lives only in generate-intent

**Where:** `plugins/mega-sdd/skills/generate-units/references/decomposition-rails.md:80` · **Lens:** dag-decomposition

**Failure:** In a multi-squad run, the model loading decomposition-rails for Step 5 follows 'Per squad-partition.md routing rules' to a nonexistent sibling path, and executes layer/hybrid routing and squads.yaml validation (duplicate-layer claims, ^squad-[a-z0-9-]+$ ID check) from memory instead of the written rules — silent divergence in squad assignment on exactly the runs where cross_squad_ambiguous should fire.

**Evidence:** decomposition-rails.md:80 ("Per `squad-partition.md` routing rules, assign `squad:`…") and unit-schema.md:267 ("`generate-units` assigns based on `squad-partition.md` routing rules") both dangle: find returns exactly one squad-partition.md, at skills/generate-intent/references/ — not in generate-units/references/ (verified directory listing). generate-units SKILL.md specialist-references block (lines 147-159) and Step 5 (line 76, "Detail: references/decomposition-rails.md §Squad assignment") never route to it; the sole cross-skill ref is vault-contract.md (line 163). plugins/mega-sdd/CLAUDE.md References standard requires the <skill>/references/X.md form and forbids sibling links whose target the SKILL.md does not route. Unreachable content: layer-hint table (squad-partition.md:13-20), hybrid feature>layer priority (:32-40), and §"Validation (performed by generate-units)" (:57-65 — ^squad-[a-z0-9-]+$ regex, duplicate owns_layers/owns_components/owns_feature_tags claims, feature-model non-empty check). No script backstop: grep for 'squad' across scripts/ matches only …[truncated]


### GU-STATE-ATOMIC [Low] — fanout-parity and starterkit-conformance write their state files non-atomically, unlike the sibling validators' AUDIT-L4 tmp+os.replace convention

**Where:** `plugins/mega-sdd/scripts/validate-fanout-parity.sh:80` · **Lens:** code-delivery-validators

**Failure:** A run-analyze (or any state-file reader) racing a parallel PostToolUse validator wave can read a truncated/torn .fanout-parity-state.json or .starterkit-conformance-state.json mid-write and mis-report the check as errored/NOT_RUN in CONSISTENCY-REPORT.md. Impact is bounded because neither file feeds the PreToolUse blocking gate — but it is exactly the torn-read class the L4 audit already fixed in the two sibling validators sharing the same dispatch site.

**Evidence:** validate-fanout-parity.sh write_and_exit (lines 79-84) does `with open(state_file, "w") as f: json.dump(report, f, ...)` directly; validate-starterkit-conformance.sh (lines 247-253) likewise pipes into `with open('$STATE_FILE', 'w')`. By contrast validate-flow-coverage.sh:164-167 and validate-sibling-consistency.sh:91-94 both carry the explicit fix comment 'AUDIT L4: atomic write (tmp + os.replace) — no torn read under concurrent bolts'. hooks/post-tool-use:445-455 spawns all these validators IN PARALLEL background jobs on every unit write, and run-analyze.sh reads the state files.


## Refuted (5)

- GU-PROBE-MEDIUM-2: IMPLEMENTED@medium row says 'apply the UNKNOWN row incl. the direct-probe rule' — the probe trivially finds the medium-anchored symbol and mints the very verify the row just forbade
- GU-RECONCILE-CONF-5: Reconcile flip rule 'claim now IMPLEMENTED → flip to verify' omits the v4.57.0 confidence gate and truncation probe — the sync lane re-opens the fuzzy-anchor verify hole
- GU-DAG-CYCLE-1: Dependency-cycle rejection is model-prose only — a cyclic depends_on ships past every deterministic validator and nothing on the execute-bolts gate path checks it
- GU-AUTHBY-1: acceptance_test._authored_by is load-bearing for --regenerate/--no-adversarial-review/Step 9.5 but is entirely undocumented in unit-schema.md's acceptance_test spec
- GU-AUTHORED-BY-7: acceptance_test._authored_by adversarial-review provenance is self-attested with no verifier, and execute-bolts uses it to suppress its weak-provenance warning

## Fix mapping (v4.58.0)

| Batch | Findings | Primary surfaces |
|---|---|---|
| 5A gate-state integrity | GU-HOOK-1/2/3/4/5 | validate-unit-spec.sh (project-wide state), hooks/pre-tool-use (gate-time re-derive + protection), validate-flow-coverage.sh + validate-sibling-consistency.sh (all-vault) |
| 5B unit-spec correctness | GU-TASKTYPE-ENUM-1, GU-VUS-A1-DOC-ANCHOR-1, GU-VUS-TF-SWALLOW-1, GU-HR-GRAMMAR-1, GU-TTCONTRACT-1, GU-VUS-AT-PRESENCE-1, GU-SK-CITE-2, GU-GCONF-1 | validate-unit-spec.sh, starterkit-derivation.md, defensive-generation.md |
| 5C flow & pack gates | GU-FC-FRONTMATTER, GU-FLOWCOV-1, GU-PACKLINT-GATES, GU-SKC-INDENT, GU-STATE-ATOMIC | validate-flow-coverage.sh, validate-pack.sh, validate-starterkit-conformance.sh, validate-fanout-parity.sh |
| 5D contract truth | GU-KEEPVAULT-1, GU-SPLIT-DEPS-4, GU-RISK-FIELD-1, GU-PBT-REJECT-5, GU-WHITELIST-6, GU-HANDOFF-DRIFT-1, GU-MODFLAG-1, GU-GRAPH-CONFLICT-1 + 7 Low | task-typing.md, decomposition-rails.md, unit-schema.md, halt-protocol.md, validation-passes.md, handoff-contract.md, auto-and-memory.md, modules-schema.md, batch-and-fanout.md, build-graph.sh |

## Fix-review round (round 2)

A second adversarial pass (5 attackers → refute-by-default verify; 22 confirmed across ~14 unique defects, 3 refuted) attacked the fixes themselves. Fixed: S5-ATK-1/FC-1/S5R-1 (High — `-bound` sibling double-count flipped real shortfalls to PASS; base vault owns the pair + realpath dedup, both validators), S5-A1-DOCANCHOR-LSTRIP (High — `lstrip("./")` char-set bug let relative `.mega-sdd/` artifact anchors ground), S5-ATK-2/S5R-2 (indent defeated the evasion net; rule-open tracking), S5-ATK-3 (decorated headings false-flagged), S5-ATK-4 (dead observable → state fields), S5-ATK-5 (*.md scoped, markdown-source projects ground), S5-ATK-6 (gate re-derive unconditional), S5-ATK-7/SC-1/S5R-7 (sibling dedup), S5-GATE-REDERIVE-DISCOVERY-MISMATCH/S5R-3 (discovery widened to every focal-accepted layout), S5-ANTISELFBYPASS-VERB-GAP (find -delete/truncate/python verbs), S5-ATK-FC-2 (numbered-steps override for coarse mermaid; DoD never overrides), S5R-1-SKC (patterns-line candidates) + S5R-5-SKC (extras field-indent lock), S5R-2-scaffold (skeleton lints clean), S5R-5-commands (honest whitelist wording). Accepted residual: SKC partial-parse mixed-indent categories (Low, downgraded — fail-loud covers the zero-parse case).

Tests: `tests/god-review-s5/test-5{a,b,c,d}-*.sh` (5A drives the REAL hooks end-to-end; 5B/5C drive the real validators against crafted fixtures; 5D contract pins + empirical build-graph).
