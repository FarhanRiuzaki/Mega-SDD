# v7.5.0 — runbook verifikasi laptop kantor (Fase 7, butir penutup)

**Status: PENDING — v7.5.0 diterima BERSYARAT "office-proven" (keputusan owner 2026-08-23).**
Dijalankan SEKALI oleh owner di satu laptop kantor (Git Bash + CrowdStrike Falcon). Seri yang diverifikasi: `66b5d00..21e4d91` (9 commit, semua CI hijau). Yang paling berisiko-platform: **№A dispatch langsung** (`b1ec253` — run-hook.sh dihapus; normalisasi `$0` backslash pindah ke tiap body sebagai `HOOK_SELF`).

## Checklist

1. **Update**: `git pull` di clone marketplace kantor (setelah leg scm ter-push) → `/plugin marketplace update mega-sdd` → `/reload-plugins` → konfirmasi versi **7.5.0**.
2. **Dispatch langsung hidup**: buka project ter-adopsi, kirim prompt biasa → tidak ada error hook di output; sesi terasa instan (UPS sekarang 1 proses, dulu 4).
3. **Gate masih fire**:
   - (a) edit inline file yang terikat claim `[LOCKED]` → notice satu baris muncul (`terikat ke claim [LOCKED] … tawarkan /mega-sdd:sync`);
   - (b) minta `rm .mega-sdd/.validation-blockers.json` via prompt → **DENY** (anti-self-bypass);
   - (c) jalankan satu chain kecil (delta lane atau `--resume`) → gate dispatch jalan, tidak ada halt aneh.
4. **Path Windows**: state file + notice bebas artefak backslash / `C:` ganda (journal `.dirty-paths.jsonl` harus repo-relative).
5. **Rasakan pajaknya**: sesi kerja normal 30–60 call — subjektif "secepat tanpa plugin"; kalau sempat, hitung kasar submit→tool-pertama. Target terukur: ≤20 s pajak hook @30–40 call; ~30 s @60 call = lantai yang diterima sampai ada keputusan binary.
6. **Hasil**:
   - **OK** → tulis satu baris di bagian bawah file ini: `office-proven <tanggal>, <mesin>` → Fase 7 TUTUP → Fase 6 (script-ification, aturan `research/2026-08-22-v7-gate6-fase4-checkpoint.md` §Fase 6) boleh mulai.
   - **Rusak** → `git revert b1ec253` (dispatch kembali via run-hook.sh dalam satu commit), push, laporkan gejala ke sesi Claude Code berikutnya. Commit lain di seri tidak perlu ikut di-revert kecuali gejalanya menunjuk ke sana.

## Keputusan owner yang menyertai penerimaan (2026-08-23)

- Deviasi **deny-tetap-python** DISETUJUI permanen — penulis JSON versi bash = kelas forgery-fragile; deny 0-python tidak akan diminta.
- **№E** (baterai deny Bash tetap bash) + **№F** (driver satu-interpreter = evidence-stop ter-gate harness parity v6.7.1) DITERIMA — parity di atas kecepatan untuk jalur langka.
- **Binary Go ditahan**; lantai ~30 s @60 call diterima sampai ada keluhan lapangan pasca-7.5.0.

## Hasil verifikasi

Dijalankan 2026-08-23 atas perintah owner di mesin owner (bukan laptop kantor — lihat catatan butir 4). Plugin yang diuji = **copy ter-install** dari marketplace (`~/.claude/plugins/cache/mega-sdd/mega-sdd/7.5.0`, sha `a6b76e0`), bukan working tree.

| Butir | Hasil | Bukti |
|---|---|---|
| 1. Update → 7.5.0 | **PASS** | `/plugin marketplace update` + `/reload-plugins` (2 hooks loaded); `installed_plugins.json` → version 7.5.0, installPath cache 7.5.0, gitCommitSha a6b76e0; `hooks/` terinstall TANPA run-hook.sh |
| 2. Dispatch langsung hidup | **PASS** | Keenam hook di-invoke live dari copy ter-install: rc 0 semua; UPS emit `mega-sdd-trace:turn` verbatim (non-SDD dir diam); pre/post/stop innocent diam; SessionStart emit anchor 3.9 KB |
| 3a. Notice LOCKED | **PASS** | Edit file ter-index → satu `additionalContext` valid-JSON ("terikat ke claim [LOCKED] … tawarkan /mega-sdd:sync"), spawn counter = **0 python**; census "udah gue commit" → tawaran sync baris kedua; trap `mudah` diam |
| 3b. DENY anti-forge | **PASS** | `rm .mega-sdd/.validation-blockers.json` via Bash → `permissionDecision: deny` (protected state file) |
| 3c. Chain kecil | **PASS** | `tests/blackbox/test-blackbox-pipeline.sh` exit 0 (13-stage e2e, gate fire live); tier-S S6a–d (stale-index fail-closed → 0-fork innocent → LOCKED deny → deny-once retry) semua PASS |
| 4. Path Windows | **PASS (simulasi)** | D3/D3b: deny byte-identik + tier-S diam di bawah `$0` backslash (ntpath); guard-path-separators, post-tool-use-windows-paths, hook-stdin-quoting semua exit 0. **Catatan: ini macOS — belum Git Bash + Falcon asli.** |
| 5. Pajak sesi | **PASS** | Terukur di copy ter-install: 30 pre + 30 post + 6 UPS = **3.8 s** (~127 ms/call, post dihitung sinkron padahal aslinya `async:true` → pajak blocking nyata lebih rendah); proyeksi 60 call ≈ 7.6 s lokal. Spawn ceilings terukur ulang hijau (C1–C9 + C6b); proyeksi Falcon ~15 s @30–40 call tetap berlaku |

**office-proven 2026-08-23, MacBook-Pro.local (macOS, owner) — dengan catatan: leg Windows/CrowdStrike terbukti lewat simulasi ntpath (D3), bukan mesin kantor fisik.** Kalau laptop kantor sempat menjalankan butir 2–3 sekali lagi, tambahkan baris kedua di sini; kalau ada gejala rusak di sana → `git revert b1ec253`, push, laporkan gejala.
