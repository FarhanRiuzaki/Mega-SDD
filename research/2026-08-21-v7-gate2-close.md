# GATE Fase 2 — ACCEPTED. Keputusan 4 item + urutan berikutnya

Fase 2 diterima: 118→87 script, hooks 4.029→3.773, session-start 22–26→0/8 fork, 10/10 merge parity-proof, trace tier S 19/19 tetap 0 fork. Merge yang memotong file bukan baris itu memang ekspektasinya — kontrak menyatu, bukan hilang.

## Keputusan

1. **Starterkit / `deep-scan-dispatch.md` → opsi (b), dibatasi.** Bedah jadi reuse-only dispatch: cabang yang memproduksi `starterkit-context.yaml` dihapus (konsumennya sudah mati), cabang `reuse-index.yaml` hidup utuh (graph code layer v6.20.0 + reuse-first adalah mandat). `starterkit-context-schema.md` dihapus; `reuse-index-schema.md` tetap. Batas: kalau bedahnya ternyata lebih dari satu commit yang bersih dengan parity reuse-index byte-identical di fixture, berhenti dan ambil opsi (a) keep utuh — jangan dipaksakan. Laporkan mana yang terjadi.

2. **`ref_loaded` → KEEP.** Investasi baru dengan test pin; memotongnya tidak menghemat apa pun yang terasa dev. Token-cost dianggap selesai.

3. **`build-dispatch-prompt` template extraction + `_lib/unit_parse.py` → dikerjakan DULUAN di sesi segar, SEBELUM Fase 3.** Alasan: Fase 3 juga menyentuh build-dispatch-prompt (string `vault_source`, nama doc), jadi kalau ekstraksi mendarat setelahnya kita mengubah file 3.7k baris itu dua kali. Kontrak: golden parity harness gate-nya, output dispatch prompt byte-identical pada fixture, satu commit untuk template, satu commit untuk `unit_parse.py` (4 kopi yaml_lite → satu), test kedua tree hijau.

4. **`xargs -P` CI → jalankan pass verifikasi no-shared-state sekarang** (grep suite yang menulis di luar `mktemp`/`$TMPDIR`), hasilnya ditempel. Kalau bersih → commit paralelisasi; kalau ada satu saja yang menulis state repo bersama → skip, catat, jangan di-fix sekarang.

## Urutan sesi berikutnya

Sesi segar: №3 (ekstraksi) → №1 (starterkit bedah) → №4 (xargs verifikasi) → lalu **Fase 3 gate**: contoh before/after vault dari `sample-prd-clinic.md` dalam layout 4 file (`vault.md` / `model.md` / `flows.md` / `constraints.md` + `vault.json`), residu 00-index ke YAML frontmatter `vault.md`, OQ terpusat di `constraints.md`, rencana migrasi via `migrate-paths` + full re-bind wajib + dual-layout read satu minor cycle. Berhenti di contoh, belum ubah konsumen.

## Catatan gue

- Leg scm: gue push sendiri saat di VPN kantor.
- CI run №16 (32563069798): konfirmasi hijau di awal sesi berikutnya sebelum lanjut.
- Pastikan `research/` punya catatan penutup Fase 2 dengan tabel before/after di atas + daftar DEFERRED (starterkit) supaya keputusan gate-0 yang dibalik tercatat dengan buktinya.
