# Laporan doc-audit v8 — 2026-09-16 (HEAD sebelum audit 4dbe798 / plugin 8.3.0 → 8.3.1)

Runbook: `research/2026-09-16-doc-audit-v8-runbook.md`. Metode §3 (klaim per klaim, sumber kebenaran = kode di HEAD), gerbang hapus §4, ukur §5, rambu §6. Verifikasi di-fan-out ke 10 agent read-only (satu ledger per permukaan, `ledgers/A0–A10` di scratchpad sesi, tidak di-commit — angka di bawah dikutip dari ledger itu); penulisan single-writer per permukaan; satu commit per permukaan (§6).

Catatan faktual yang mengoreksi asumsi runbook (kode > dokumen):
- **Lane classic masih DEFAULT sepanjang 8.x** (CHANGELOG `## [8.0.0]`: kriteria (a)/(b) gagal → `--lite` opt-in). Jadi `binding.md`, `claims-ledger.json`, vault 4-file layout-2, dan bind sebagai fase **hidup** di lane classic. Istilah itu bukan mati; yang mati = menyajikannya sebagai satu-satunya lane. Perbaikan = kualifikasi lane, bukan hapus.
- **Legacy 7-file = read-only** sepanjang 8.x (resolver `_lib/vault_md.py` layout-3 → layout-2 → legacy; 20 script masih membaca `0[0-6]-*.md`). Dokumen migrasi/qualified "legacy 7-file" dipertahankan.
- **tree-sitter benar-benar mati sebagai engine** (`probe-scan-engine.sh` hanya `--engine=ast-grep|regex`, `ground.sh:329`); yang tersisa di kode = komentar + key pass-through `tree_sitter_version`. `tool-matrix.yaml` sudah tidak punya row tree-sitter.
- `architecture-advisor.md` = fitur HIDUP (dikonsumsi extract-intelligence + generate-intent --kb + test). "advisor" yang mati = phase-advisor (v7.4.0). Grep `advisor` polos = 142 hit karena mencocokkan "advisory" (kata biasa).
- `run-hook.sh` memang tidak ada (hooks.json dispatch langsung).

## 1. Tabel file × klaim dicek × salah × diperbaiki (per permukaan §2)

| # | Permukaan | File | Klaim dicek | Salah | Diperbaiki | Commit |
|---|---|---|---|---|---|---|
| 1 | skills + commands (router/using/analyze/graph + 6 command) | 23 | 337 | 52 | 62 | `e29d9fe` |
| 1 | skills generate-intent / resolve-oq / diff-vault | 32 | 395 (+348 pass kedua independen, 74 salah, tumpang tindih) | 81 | 83 + 26 delta | `e29d9fe` |
| 1 | skills bind-codebase / generate-units / plan / detect-drift | 33 | 405 | 35 | 30 | `e29d9fe` |
| 1 | skills execute-bolts (SKILL + 17 ref) | 18 | 325 | 34 | 41 | `e29d9fe` |
| 1 | skills scan-codebase / extract-intelligence / install-deps / emit-* | 27 | 355 | 42 | 48 | `e29d9fe` |
| 2 | references/*.md + halt-families + lib-patterns + design-intelligence (pointer) + docs/gateway-contract.md | 42 | 406 | 57 | 55 | `f41107c` |
| 3 | agents/*.md | 9 | 145 | 3 | 3 | `f7b319b` |
| 4 | references/framework-conventions | 30 | 219 | 17 | 51 | `e55e037` |
| 5 | plugins/mega-sdd/CLAUDE.md + CLAUDE.md root | 2 | 65 | 7 | 8 | `8c0c7da` |
| 6 | README root + plugin README + CONTRIBUTING | 3 | 148 | 18 | 23 | `13c5be6` |
| 7 | tests/scenarios (12 + README + 4 sample) | 17 | 253 | 60 | 55 | `599e45e` |
| 8 | docs/mega-sdd/* + plugin.json description + marketplace.json (+ CHANGELOG entry) | 11 | 172 | 16 | 26 + 3 stempel + description | `48ea26b` |
| — | docs/superpowers/specs stempel "Superseded" | 11 | — | — | 11 stempel (1 baris) | `7e6aa57` |
| **Σ** | | **≈258** | **3.225** (3.573 termasuk pass kedua A2) | **422** | **≈510** | 12 commit |

Angka "salah" = verdict FALSE di ledger (klaim yang bertentangan dengan kode/fs atau menyebut hal yang tidak ada). OPEN (tidak bisa diverifikasi murah) ≈ 150, dicantumkan di ledger; yang berbobot ada di §7.

Kelas kesalahan terbesar (lintas permukaan): (1) satu lane disajikan sebagai satu-satunya lane / layout-3 `context.md` tidak disebut sama sekali (resolve-oq/diff-vault STOP di vault plan-born; README/architecture/reading-map/upgrade guide tanpa 8.0.0) — ±110 site; (2) mekanisme mati sebagai hidup: PostToolUse validator fan-out (v7.5.0) ±20 site, memory lane/`.mega-sdd/memory/*` ±25, tree-sitter sebagai engine/tier 17, enrich-semantics 8, checkpoint `--resume-from` sebagai perilaku live, vendored fallback, `Hard Rules active`/trailer 5 baris; (3) flag/section/path yang tidak ada di parser: `validate-unit-spec.sh --vault=` (LIVE gagal `unknown arg` di 5 stream xs 7.38.0→8.3.0), `write-unit-binding.sh --resolve <id>=` (butuh `=`), `generate-units --units=`, `--reuse-bolt-snapshots`, `--per-bolt --unit=`, `--auto-gate`, `--scope-confirm-timeout`, `list-modules` sebagai command, `migrate-rules --vault=`, 9 pointer `vault-contract.md §…` (section pindah ke vault-core.md), `hard-rules-and-packs.md §2.9a`, `chain-execution.md §Phase context`; (4) nama doc 7-file sebagai target hidup (`02-architecture`, `04-flows`, `[[03-data-model]]` wikilink yang di-cp ke setiap vault) ±45 site; (5) skema/enum salah: RESOLUTIONS `id | verdict | file:line` vs parser `id | file:line | verdict` (finding tidak pernah tertutup), `test_fail` details, `starterkit_rule_citation_missing` tanpa envelope, `suggested_resolutions` vs `suggested_action`, `accessor_template` vs `accessor_form`, `ast_grep_pattern` (0 pemakai), Obsidian rgb.

## 2. Daftar hapus (gerbang §4)

**Tidak ada file yang dihapus.** Tidak ada kandidat yang lolos keempat gerbang sekaligus, dan tidak satu pun file runtime terbukti nol konsumen (setiap ledger menjalankan sensus konsumen: minimum 1 konsumen — `detection-and-shapes.md` hanya dari SKILL.md-nya sendiri; `starterkit-enrichment.md` 0 pin test tapi dirute SKILL.md + builder). Yang dihapus hanya **isi di dalam file**: §Backward compatibility v0.13 di halt-protocol (438 B), skema preflight/postflight yang tidak pernah dikirim di shared-snapshot-schema (−825 B), blockquote hook fiktif + row Known-open PostToolUse di context-enrichment, 2 row halt tidak terdaftar di modules-schema, kalimat "Memory consultation", bullet memory di audit-and-verify, `--scope-confirm-timeout`, "Limitation *-bound only" ×2.

## 3. Daftar pindah

**Tidak ada file yang dipindah.** Tidak ada yang salah tempat secara struktural; yang salah adalah isi.

Stempel (bukan pindah, bukan tulis ulang): 11 spec lama (`2026-05-13-mega-sdd-revamp-design`, `…v1.1-alignment-oq-deferral`, `2026-05-21-memory-self-learning`, `2026-05-24-iter-33-flawless-seamless-intelligence`, `2026-05-26-iter-63-performance-sharpness`, `2026-06-04-v4-lean-core` (catatan baseline), `2026-06-10-learning-loop`, `2026-06-12-compaction-advisor`, `2026-07-30-token-and-latency-optimization`, `2026-08-12-playwright-embed-design`, `2026-08-12-context7-embed`) + 3 rekaman bertanggal di docs/mega-sdd (`feedback-response-2026-08-23`, `feedback-response-2026-08-31`, `keputusan-arsitek-gateway`).

## 4. Grep daftar istilah mati §3 — sebelum → sesudah (runtime = skills/ references/ agents/ commands/ hooks/; hitungan baris)

| Istilah | Sebelum | Sesudah | Klasifikasi sisa (semua diperiksa satu per satu di ledger) |
|---|---|---|---|
| `7-file` | 14 | 15 | LANE-VALID/MIGRATION: "legacy 7-file" read-only, rung migrasi; +1 = klausa layout-3/2/legacy yang ditambah |
| `4-file` (sebagai layout aktif) | 11 | 10 | LANE-VALID: layout-2 classic default; STALE 0 |
| `binding.md` / `claims-ledger.json` (artefak hidup) | 185 / 13 | 188 / 13 | LANE-VALID (classic lane hidup); qualifier lane ditambah di deskripsi/When-to-use; STALE 0 |
| bind sebagai fase pipeline | (tak ter-grep) | — | kualifikasi "classic lane only + FATAL `bind_folded_into_bolts`" di bind-codebase/generate-units/generate-intent; STALE 0 |
| `telemetry` / `observability` | 4 / 6 | 4 / 6 | kode hooks (komentar removal) + FRAMEWORK (Horizon, product-style-map); STALE 0 |
| `token-cost` / `TOKEN-COST-REPORT` / `memory review` | 0 | 0 | — |
| `slice-design` | 1 | 1 | LIVE: row plugin `mega-sdd-extras` di paths.md |
| `advisor` (word-boundary) | 20 | 15 | LIVE architecture-advisor + NEGATIVE-RAIL phase-advisor removal; "advisory" = kata biasa |
| `vendored` | 13 | 12 | NEGATIVE-RAIL ("removed v7.4.0") + FRAMEWORK (`vendor/`); STALE 0 (description execute-bolts diperbaiki) |
| `tree-sitter` | 53 | 35 | 27 file: komentar pack `queries/astgrep/*.yml` tentang nama node grammar (ast-grep berbasis tree-sitter — FRAMEWORK), removal notes; STALE 0 (17 site engine/tier diperbaiki) |
| `run-hook.sh` | 7 | 7 | 6 komentar kode hook "dispatcher is gone" + 1 tooling-install "direct dispatch since v7.5.0" (NEGATIVE-RAIL) |
| `Hard Rules active` | 3 | 3 | NEGATIVE-RAIL "do NOT add back" (agent + 2 ref), dipin test |
| trailer 5 baris / `seeding_budget` | 0 | 0 | — |
| `starterkit-metrics` | 1 | 1 | komentar kode hooks/post-tool-use |
| (tambahan, ditemukan audit) PostToolUse sebagai trigger validator | ±26 site | 0 STALE | sisa 26 baris = hook hidup (journal/notice), removal notes, komentar |
| (tambahan) `enrich-semantics` | 8 | 5 | tombstone "REMOVED v7" + routing ke tombstone; STALE 0 |
| (tambahan) memory lane (`.mega-sdd/memory/*`, "per memory", "logs to memory") | ±25 | 0 STALE | sisa = `memory_in_use` (halt hidup), `.memory/bolt-outcomes.json` (file hidup, dibaca query-graph), removal notes |

Dokumen manusia (README ×2, CLAUDE ×2, scenarios, docs/mega-sdd): sisa `binding.md` 22 (qualifier lane ditambah), `7-file` 11 (migrasi/legacy), `tree-sitter` 4 (removal notes), `run-hook.sh` 3 (removal notes), `observability` 11 (gateway stack + "gateway-only"), `PostToolUse` 16 (hook hidup / "fan-out dihapus"). STALE 0. Target "NOL" tercapai dalam arti runbook: nol klaim v7-atau-lebih-lama yang disajikan sebagai hidup; istilah yang tersisa semuanya rekaman, rail negatif, kata framework, atau artefak lane classic yang memang hidup.

## 5. Delta byte per lane (§5) — tracer `benchmarks/scripts/measure-context.sh` (static trace daftar `files.<arm>.txt`, MEASURED bytes) sebelum (4dbe798) → sesudah (HEAD)

| Arm | bytes_loaded Σ task | Δ | median est_tokens |
|---|---|---|---|
| `baseline` (8 task, v6 list, 20 path legacy missing — sama sebelum/sesudah) | 2.764.482 → 2.779.889 | **+15.407 (+0,56 %)** | 94.420 → 94.954 |
| `optimized` = lane default classic (10 task) | 2.820.155 → 2.838.847 | **+18.692 (+0,66 %)** | 60.925 → 61.264 |
| `lite` (T01 saja; daftar `files.lite.txt` STALE: masih chain generate-intent/units, bukan plan) | 479.178 → 481.727 | +2.549 (+0,53 %) | 118.548 → 119.172 |
| lite-v8 (daftar turunan dari SKILL.md 8.3.0: front door → plan + owning contracts → execute-bolts --lite, 29 file, `[SECTION:]` = upper bound; disimpan di scratchpad, bukan benchmarks/) | 452.735 → 455.243 | +2.508 (+0,55 %) | 113.184 → 113.811 |

Total permukaan runtime (semua .md): skills 1.719.557 → 1.729.664 (+10.107), references 336.431 → 337.149 (+718), agents 77.609 → 78.070 (+461), commands 46.592 → 46.699 (+107), framework-conventions 512.120 → 517.885 (+5.765).

**Verdict jujur §5:** audit ini **tidak menurunkan byte yang dimuat — naik ±0,6 %**. Pemangkasan (skema mati −825 B, backward-compat −438 B, row/kalimat mati ±2 KB) kalah oleh penambahan klausa kualifikasi lane + layout-3 + pointer yang benar. Ini pass **kebenaran**, bukan diet; secara byte tergolong kosmetik-negatif dan disebut apa adanya. Diet byte butuh keputusan owner tersendiri (lihat [OPEN] archaeology versi).

## 6. Replay skenario (§2 row 7: minimal SATU replay nyata)

- `bash tests/blackbox/test-blackbox-pipeline.sh` (script nyata, 0 model): **43 ok / 0 failed, VERDICT: READY TO SHIP**. Mengonfirmasi janji skenario-6/2/3: gate `bind_conflict` (make-bound menolak exit 2 saat CONFLICT, bound/ tidak dibuat), tamper-then-mint preflight exit 8, pelanggaran ter-commit tertangkap postflight (MISMATCH), artefak `bolt-report.md`/`preflight.json`/`postflight.json`/`acceptance.json` ada (35 file), citation-map halt pada path fabrikasi + drift terdeteksi, JIT bind per unit (S14: CONFLICT → `.validation-blockers.json` FAIL → resolve → PASS).
- Playground (`tests/blackbox/seed-playground.sh`) + `ground.sh --cwd`: status view `position=prd_no_vault … next: (none)` — bentuk yang dijanjikan skenario-0 (pre-init: probes only).
- Replay mengungkap bug kode (dilaporkan, tidak diperbaiki — §6 rambu): lihat §7 #7 (`validate-scope-flag.sh` bentuk MAP) — ditemukan saat menjalankan sample PRD melalui script.

## 7. Temuan bug KODE untuk gate berikutnya (tidak disentuh; urut dampak)

1. **`agents/resolution-verifier.md` vs `merge-panel-findings.sh`** — urutan kolom RESOLUTIONS terbalik → verifier yang patuh prompt tidak pernah menutup finding; setiap fix-round jatuh ke budget/karantina. Doc diperbaiki (prompt = doc), parser tetap; owner boleh pertimbangkan parser toleran.
2. **`validate-unit-spec.sh` tidak punya `--vault=`** — plan SKILL/procedure menyuruh memakainya; LIVE `unknown arg` rc=2 di 5 run xs. Doc diperbaiki; pertimbangkan menerima `--vault=` (filter atau no-op).
3. **`validate-preflight.sh _layout3_vault_present()` project-wide** — satu vault layout-3 membuat bind/units FATAL untuk SEMUA vault (multi-vault/multi-PRD).
4. **`build-fsd-core.sh:236`, `build-prd-core.sh:353,370` membaca `<vault>/_meta/constitution.md`** — semua producer menulis `<vault>/constitution.md` → klausa konstitusi tidak pernah muncul di FSD §6.4 / PRD §5.
5. **`ground.sh` menandai `model_tiers.bolt_implementer` sebagai `model_tier_unknown`** (katalog dibaca hyphen, config underscore, tanpa normalisasi) — LIVE-proven.
6. `ground.sh:91-95` melewati vault.json korup tanpa halt ("future slice"); paths.md sebelumnya mengklaim "detects".
7. **`validate-scope-flag.sh`** parse `scopes:` hanya inline/list, bukan bentuk MAP kanonik (template + 3 sample) → `--scope=XX` PASS "no scopes block"; juga blok stdin tanpa `--user-message-file`.
8. **`publish-artifacts.sh`** tidak pernah mengirim `bolts/U-*/binding.json` → project lite tidak publish verdict ke gateway (re-key 8.0.0 tidak sampai publisher).
9. `run-preflight-scan.sh:321` ast-grep TIMEOUT → exit 3 (= `hard_rule_unparseable`) — salah kelas.
10. `validate-handoff-yaml.sh` tidak memeriksa tipe halt terhadap registry (registry mengklaim; doc dikoreksi).
11. `write-unit-binding.sh --resolve` menolak DEFER sementara binding-mode menawarkan [D] untuk konflik per unit.
12. Hint mati di kode: `validate-preflight.sh:462-465` `generate-intent --refresh`; `validate-vault-flow-staging.sh:188`, `validate-kb.sh:936` `enrich-semantics`.
13. Pack: `accessor_template` (laravel/slim/symfony) tidak dibaca (validator baca `accessor_form`) → jalan di `any` permisif; `## Security idioms` tidak pernah sampai `build-dispatch-prompt.sh` (hanya lens security).
14. Regex sanctioned-extras B3 lebih sempit dari instruksi implementer (`foo.test.py` → `whitelist_violation`); doc disamakan ke regex — keputusan desain.
15. `state_probes._PRD_SUBDIR_NAMES` tidak memindai `source/` tempat from-prompt menulis `seed-PRD.md`.
16. Predictive checks `binding_present_for_drift` / `vault_present_for_oq` bisa FATAL hop lite (belum diverifikasi); delta lane (chat brief) tidak punya rendering lite.
17. `resolution_source` / `recommendation_citation` didokumentasikan + diharapkan test, tanpa writer.
18. `run-full-suite.sh` tidak menulis `bypass_commits[]` (doc dipindah ke `_summary.md`).
19. `validate-bolt-artifacts.sh:1400-1408` resolusi cite ADR PBT hanya `D-*.md` (layout-2/3 `### D-NNN` mungkin false-positive) — belum diverifikasi.
20. `ci-recipe` Recipe 3 `grep stale=0` tidak pernah cocok (doc diperbaiki ke JSON).

Test/fixture (temuan, tidak dihapus/dilonggarkan): tidak ada `tests/skill-triggering/plan.test.md`; `execute-bolts.test.md` BH5/BH8 masih topologi squad-subagent lama; `seed-playground.sh` mencetak `/mega-sdd:auto`; golden `dispatch-parity/f2-ui` bawa pointer "Fork A"; `scope-picker.test.md` + scenario-7 "default after 5s"; fixture `code-delivery/*/.cache/pack-resolver` bawa `accessor_template`; `resolve-oq.test.md:176-184` mengharap field tanpa writer. Satu fixture disinkronkan: `resolve-oq.test.md:79` "seven documents" → set per layout (pasangan wording SKILL; tidak melonggarkan invariant).

## 8. [OPEN] (tidak ditebak)

- Registry halt: 6 tipe skill-emitted belum terdaftar (`drift_inputs_missing`, `scan_spawn_budget_exceeded`, `scan_repo_too_large`, `scan_primary_app_ambiguous`, `codebase_map_derive_failed`, `codebase_map_invalid`), 4 halt emit-agents-md, subtype `claim_verify_failed`, `path_stale_pending_restart`, `scope_args_missing`, `routing_outcome_corrupt`, kode hook-gate (`binding_missing`, `conflict_unresolved`, …) — perlu keputusan owner apakah masuk registry + taxonomy (test mirror mengikat).
- `next_action.type`: 9 dari 12 nilai enum tanpa emitter/consumer (kini dilabel; hapus = keputusan owner).
- `partial_state_corrupt`: registry C1 self-resolve vs partial-state-and-saga "ALWAYS STOP".
- `.mega-sdd/project.md` (using-mega-sdd:75) tanpa producer; `_diagnostics/kb-skipped-artifacts.md` tanpa reader; `<vault>/.mega-sdd/vault-diffs/*.patch` tidak ada di paths.md; `halt_auto_propose` config prose-only; `emit_agents_md` config key ditulis migrate-paths tapi tidak didokumentasikan.
- `benchmarks/tasks/T01-greenfield-chain/files.lite.txt` STALE (chain classic + jit-bind, bukan plan) — benchmarks tidak disentuh (§4); daftar lite-v8 turunan ada di scratchpad + §5.
- Archaeology versi di body skill (`(7.21.0)`, `(v8 P1.e …)`): tidak dipurge (standar authoring menyasar description); densitas tinggi = kandidat diet byte, keputusan owner.
- Angka pengukuran lama di doc (`~5s vs ~28s`, `29x`, `±150-220k token`) tidak diulang.
- Klaim eksternal (CLI `claude plugin`, Node floor, versi framework) tidak diverifikasi dari repo.
- `docs/mega-sdd/architecture.md` + `fork-a-recovery-map.md` tidak ditautkan dari README (bukan orphan: revamp-journey menaut architecture).

## 9. Verifikasi

- Suite lokal dua tree SEBELUM: 458 suite, 0 merah (rc=0). SESUDAH: lihat baris NEXT (dijalankan ulang pada HEAD final; hasil di CHANGELOG/commit laporan). Setiap permukaan juga memanggil suite pin-nya sendiri saat ditulis (A1 54/54, A2 14/14, A3 17/17, A4 22+, A5 19/19 + 9 pin, A6 59/59, A7 22/22, A8 10/10, A9 8/8, A10 test-6d + size-weighted, CLAUDE.md 8/8) — semua hijau; dua kali test pin membatalkan/menggeser edit gue (halt-enum sync → taxonomy diklasifikasi; keterangan literal `DELIBERATE chain-context divergence` dipulihkan; satu kesalahan gue sendiri — pemangkasan halt-protocol yang salah batas 24,9 KB — tertangkap `test-family-split` dan dipulihkan dari git sebelum commit).
- `claude plugin validate plugins/mega-sdd` PASS (warning root CLAUDE.md = kontrak sengaja); parity CHANGELOG = plugin.json = marketplace.json = 8.3.1; `validate-pack.sh --all` + `--check-registry` OK.
- Leg scm TIDAK di-push (§6); leg GitHub di-push untuk CI.

**NEXT SESSION:** gate kode spec-first untuk bug #1–#8 (RESOLUTIONS parser toleran, `validate-unit-spec.sh --vault=`, `_layout3_vault_present` per-vault, `_meta/constitution.md` path, `model_tiers` normalisasi, vault.json korup, `validate-scope-flag.sh` MAP, publisher `binding.json`), lalu keputusan owner registry 6 halt + archaeology versi (diet byte).

## 10. Verifikasi final (ditambahkan setelah suite + CI pertama)

- Suite lokal dua tree pada HEAD `c72ecbb`: 458 suite, **2 merah** — `tests/extras/test-extras-plugin-contracts.sh` + `tests/playwright-embed/test-playwright-embed-contracts.sh`, keduanya pin BYTE anchor core `using-mega-sdd` (baseline 3844 B / compact 1494 B; cap 4030) yang kena +125 B dari kualifikasi lane pada baris **Hard gate** (`binding.md` … classic/lite). CI GitHub run 35075745867 merah pada dua suite yang sama. Fix: baris anchor dikembalikan ke wording semula (test TIDAK disentuh — §6); kedua suite kontrak + p6-front-door + weighted-routing + anchor-diet hijau setelah revert. Temuan tersisa: baris Hard gate di anchor tetap classic-only karena byte-pinned — re-baseline pin = keputusan owner (ruang +186 B di bawah cap).
- Pelajaran: dua surface (`using-mega-sdd` ANCHOR-CORE, `execute-bolts` dispatch golden) dipin per BYTE, bukan per klaim — audit klaim harus cek pin byte sebelum menyentuhnya.
- Commit fixup ini = commit ke-14 (permukaan 1 mendapat satu commit fixup di luar aturan satu-commit-per-permukaan; dicatat jujur).
