# Audit dokumentasi v7.5.x — klaim-per-klaim, semua permukaan vs kode di HEAD

**Mandat owner (2026-08-23):** docs harus 1:1 dengan v7.5.x, metode klaim-per-klaim (ekstrak → verifikasi terhadap kode → perbaiki), bukan baca-rapikan. Sumber kebenaran = kode di HEAD. `research/` + spec berstempel = rekaman sejarah, tidak ditulis ulang (kecuali satu stempel supersede bila berisiko dibaca sebagai kebenaran kini). Satu commit per permukaan.

**Metode eksekusi:** 6 agent audit read-only paralel (satu per permukaan) mengekstrak + memverifikasi tiap klaim dengan grep/ls/eksekusi; temuan diverifikasi ulang untuk item load-bearing; perbaikan diterapkan per permukaan; 1 scenario di-REPLAY nyata (lihat §Replay). Ground truth ditera dulu: **6 command** (3 verb + 3 one-timer) · **19 skill** · **8 agent** · **6 hook event** dispatch langsung (SessionStart, PreToolUse `Skill|Bash|Edit|Write`, PostToolUse `Write|Edit`, Stop, UserPromptExpansion, UserPromptSubmit) · vault layout-2 4-file.

## Hasil per permukaan

| Permukaan | Klaim dicek | Salah | Diperbaiki | Catatan |
|---|---|---|---|---|
| Root `README.md` | 78 | ~24 | ✅ semua | Kelas dominan: lane memory (dihapus v7.3.0) masih dijual sebagai fitur headline di 11+ situs — bullet, diagram Mermaid (node MEM), cheat-sheet, folder layout, flag `--memory-off`; "20 skills"→19; "4 verbs"→3; pitch scan→ground; phases 5→4 express; `MEGA_SDD_ROUTING` opt-out mati; Aider `.scm` attribution di-historisasi |
| `plugins/mega-sdd/README.md` | 64 | 14 | ✅ semua | "commands exactly 8 (… slice)"→6; skills 20→19; `resolution-verifier` hilang dari tree; hooks 4 event→6 + no-run-hook; defense list: nomor 9 bolong + `conflict_classification` (boundary dihapus Fase 2) → diganti gate bukti B1–B4+orphans+ledger; `layout: canonical`→`new`; config contoh diperluas (auto_verify_on_edit, parallel_max, model_tiers); What's new v7.1–7.5.x ditambah; chooser 13→12 |
| `CLAUDE.md` ×2 (kontrak) | 74 | 8 (root: 0) | ✅ semua | Root bersih 11/11. Kontrak plugin: fan-out PostToolUse masih diklaim hidup (dihapus №D) → ditulis ulang + 6 event + matcher + doktrin biaya hook; gate bolt "four"→**five** (bolt-orphans tak pernah disebut); `model` per role → pengecualian `bolt-implementer: inherit` (router per-unit); split-root `tests/` dieksplisitkan; scan/bind sudah fork-contract; klausa `_vendored` self-contradiction dibereskan |
| `tests/scenarios/` (13 file) | 267 | ~82 | ✅ semua | Verdict: 1 OK (scenario-12), 11 FIXABLE, 1 REWRITE-kelas (scenario-6). Kelas: vault 7-file→layout-2 (11 situs), `[auto]` diagnostik classic-only ditampilkan sebagai default (17 baris), lane memory (Step 8 s1, §Memory growth s4, 2 seksi s6), **9 flag fiktif** di scenario-6 (`--allow-gaps`, `--resolve=`, `--single-scope`, `--retrofit-scopes`, `--accept-low-confidence-retrofit`, `--emit-blocker`, `--confirm-locked-drift`, `--strict-self-assessment`, `--allow-deferred`), **1 instruksi DESTRUKTIF** (echo teks ke `bolt-outcomes.json` yang masih dibaca script — dihapus), scenario-9 = dead link di chooser (row dihapus; gap, bukan renumber), scenario-0/1 first-run tidak menarasikan halt `no_starterkit_detected`, scenario-11 semua hitungan katalog salah (17→23 role) + fitur v7.1 yang jadi judulnya justru absen (Example 5 ditambah) |
| `references/` + `docs/gateway-contract.md` (14 file) | ~260 | ~35 | ✅ semua | Terbesar: halt-protocol enum↔index desync DUA arah (20 type hidup absen dari enum → tereject `invalid_handoff` menurut aturannya sendiri; 7 type tanpa row; `source_skill` kurang emit-prd/sit/uat, masih muat `memory`); `memory_in_use` body = framing memory + **angka retry fabrikasi** (10×/40s vs kode 3×/2.1s exit 4); model-tiers §user-scope preferences.md mati + distribusi salah; tooling-install "5 hook/9 entri, semua spawn python" → 6/6, 2 pure-shell; output-language census masih vocab legacy → dual-layout + tag gateway & commit-trailer masuk census; paths.md ±10 klaim mati (3 row memory, `default_output_root`, `layout:` no-reader, `.ptu-scan-stamp`, `exports/`) + **§Root state surface baru** (~35 state file moat akhirnya punya rumah); gateway-contract ditambah detail census/manifest/gate publish |
| `docs/mega-sdd/` (7 file) | 199 | ~20 | ✅ semua | **Temuan terbesar audit**: pasangan gateway (`gateway-mcp-guide.md` §8 + `keputusan-arsitek-gateway.md` §Governance) masih memerintahkan tim gateway meng-key enforcement warn→block ke `mega-sdd-trace:session` — marker dengan **0 emitter** (retired v7.3.0, dipin NEGATIF), plus opt-out `trace_tag`/`MEGA_SDD_ROUTING` yang tidak pernah ada di kode → ladder ditulis ulang ke kontrak `:turn` hidup (baris-1 verbatim, agregat per NIP per window) + aturan baris-kedua census v7.5.0; error "vault 7-file" sistemik 3 file (terburuk: kontrak path artifact untuk indexer gateway → indexer yang dibangun dari situ menemukan NOL file); upgrade doc dapat §7.3–7.5 (kosong 4 rilis), target v3.41.0→7.5.x, langkah "migrate memory" mati dihapus; fork-a-recovery: `/analyze` "Not implemented"→implemented sebagai SKILL; test `trace-governance-contract.test.sh` yang dikutip sebagai jaminan TIDAK ADA → sitasi diganti test hidup |
| Manifests (`plugin.json` desc + `marketplace.json`) | 6 | 0 | — | Dicek ulang: deskripsi masih akurat pasca-Fase 5; versi = 7.5.2 (bump karena bug kode di bawah, bukan karena deskripsi) |
| Spec berstempel | — | 1 stempel | ✅ | `docs/superpowers/specs/2026-08-17-artifact-publisher-gateway.md` — satu baris "Superseded in part by v7.3.0" (kontrak `:session` §v6.19.2) karena berisiko dibaca sebagai kebenaran kini oleh tim gateway |

**Total: ±950 klaim diverifikasi, ±184 salah, semua diperbaiki** (di luar temuan bug kode di bawah).

## Replay nyata (mandat: minimal satu scenario)

Greenfield/clinic di-replay lewat jalur deterministik produksi: `test-blackbox-layout2.sh` dijalankan dengan workdir dipertahankan — L1 seed → migrate `--vault-layout --apply` → claims-ledger → validator live → binding + **CONFLICT gate FIRED live** (make-bound refuse exit 2) → resolve → bound/ → unit → flow-coverage. Semua hijau. Tree vault NYATA yang tertangkap (`vault.md`, `model.md`, `flows.md`, `constraints.md`, `vault.json`, `binding.md`, `bound/{4 doc+binding}`, `units/U-001.md`, `claims-ledger.json`) menjadi ground truth rewrite scenario. Disclosed: langkah chain interaktif (`/mega-sdd` + OQ) diverifikasi statis terhadap `commands/mega-sdd.md` (flag census lengkap), bukan live-chat run.

## Temuan BUG KODE (bukan dok) — sesuai rambu: dilaporkan, TIDAK diperbaiki di seri dok

1. **FIXED terpisah sebagai v7.5.2 (regresi sesi ini sendiri, bukan scope-creep):** `hooks/session-start:102` masih men-debounce marker wrapper pada literal `v1` padahal 7.5.1 menaikkan installer ke `WRAPPER_VERSION=2` — wrapper v1 tim **tidak pernah ter-upgrade** lewat jalur produksi. Kelas pelajaran standing persis: test wrapper memanggil installer langsung, bukan hook yang men-dispatch-nya; DAN dua harness tier-S/spawn **men-seed fixture wrapper `v1` hardcoded** sehingga mismatch tak terlihat suite. Fix: literal→v2, kontrak 7 (parity statis + behavioral lewat HOOK + debounce mtime-stable), fixture harness derive dari `WRAPPER_VERSION`.
2. **OPEN — `scripts/build-prd-core.sh:324,330,333` membaca `03-open-questions.md`**, nama file yang tidak ada di layout mana pun (layout-2: `constraints.md ## Open Questions`; legacy: `06-constraints.md`). Lane emit-prd kehilangan slot OQ pada vault layout-2.
3. **OPEN — model-tiers katalog row 11–14, 18** (`pipeline-audit-per-skill`, `pipeline-audit-consolidator`, `intelligence-audit-deep`, `intelligence-audit-probe`, `domain-research`): NOL situs dispatch mengutipnya, tapi baris TIDAK disentuh karena `ground.sh:396+` mem-parse baris tabel jadi `catalog_roles` (runtime) — menghapusnya = perubahan perilaku yang butuh spec. `domain-extractor` (agent nyata) justru tanpa row.
4. **OPEN — GROUND C1 battery glob `*-bound/` sibling legacy saja** (`ground.sh`, arm `partial_state_corrupt` + `verify_unit_writable`): vault layout kanonik tidak ter-scan oleh rung self-resolve itu (dicatat jujur di halt-families/bolts.md).
5. **OPEN — `skills/analyze/SKILL.md:17`** masih mengklaim state ditulis "PostToolUse validators during the session" (fan-out dihapus №D); + 2 komentar kode basi (`pre-tool-use:764` "PostToolUse-written state", `:804` "SIX" seharusnya SEVEN). Skill = surface operatif → butuh spec-trace kecil, bukan edit seri dok.
6. **Non-bug, tercatat:** `tests/blackbox/seed-playground.sh` tertangkap filter suite `find *.sh` padahal bukan test (non-idempoten: run kedua fail "already exists") — kandidat exclude di runner.

## Pelajaran audit (standing)

- **Dok yang saling mengutip sama-sama salah**: error "vault 7-file" hidup serempak di 3 dok karena saling menyalin; `reading-map.md:51` satu-satunya yang benar dan jadi template fix. Verifikasi selalu ke kode.
- **Fixture bisa meng-encode bug**: harness men-seed literal `v1` → mismatch versi lolos semua suite. Fixture yang menyalin konstanta produksi harus DERIVE, bukan hardcode.
- **Dok referensi bisa runtime-parsed**: `model-tiers.md` dibaca `ground.sh` (tabel = `catalog_roles`) — cek konsumen sebelum mengedit "dokumen".
- Kelas terbanyak: fitur dihapus (memory/telemetry/slice) yang dokumennya tidak ikut dihapus — removal PR wajib menyapu permukaan dok user-facing, bukan cuma CHANGELOG.

## Commit (satu per permukaan)

`fix 7.5.2` (kode, terpisah dari seri dok) → root README → plugin README → CLAUDE.md → scenarios → references+gateway-contract → docs/mega-sdd+stempel spec → laporan ini. Suite penuh dua tree di tree settle + CI per push.
