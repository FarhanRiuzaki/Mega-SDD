# GATE Fase 3 — ACCEPTED. Dua gate berikutnya (read-only)

Fase 3 diterima. Deviasi bentuk (changelog/sources/auto-classification sebagai section md, bukan YAML) **disetujui dan justru lebih benar** — mentranskode md authored ke YAML memang risiko fabrikasi, dan grammar `### vN.N` yang dibaca resolve-oq/diff-vault tetap utuh; Amendemen 2 hanya mewajibkan lock scalars. Bonus Mermaid menolak fixture sendiri = validator bekerja; bagus bahwa kontennya yang dibetulkan, bukan gate-nya.

Angka −11% (fixture ringkas) / −42% (template ceiling) dicatat apa adanya — lower bound yang jujur lebih berguna daripada klaim. Dual-layout read: pencabutan fallback diputuskan di minor berikutnya dari telemetry, bukan sekarang.

Satu permintaan kecil: tulis `research/2026-08-22-v7-program-summary.md` satu halaman — before/after seluruh program v7 (Fase 0–3): routing, hooks, script, vault, angka terukur, dan daftar keputusan yang dibalik oleh bukti (starterkit, №3, marker cursor→chain_engaged, YAML→section). Ini jadi dokumen yang gue bawa ke tim kantor.

## Sesi berikutnya — dua pekerjaan, dua-duanya berhenti di gate, nol perubahan kode

### A. Desain "per-unit model routing + cascade"

Baca `research/2026-08-22-smart-routing-research.md` dan jalankan prompt §6 persis. Tambahan dari gue:
- Langkah pertama: **verifikasi empiris** apakah Agent tool di build Claude Code saat ini menerima parameter `model` saat spawn (coba satu dispatch dummy dan catat hasilnya) — jangan asumsi dari docs.
- Router harus memakai `resolve-review-tier.sh` yang ada (tambah field, bukan script baru). Rail A5 tetap: deterministic evidence, never model self-assessment.
- Tidak ada persona prompt, tidak ada agent baru selain varian implementer, tidak ada perubahan gate anti-halu.
- Output: desain ≤2 halaman + file yang disentuh + risiko + rencana A/B di `sample-prd-clinic.md` dengan kriteria ship ≥25% hemat token, kualitas panel setara.

### B. Scoping audit Fase 4 — markdown diet (metrik: token commanded per lane)

Sekarang scripts sudah tidak masuk context, yang dimuat LLM adalah `skills/*.md` (±1,8 MB) + `references/` (±0,8 MB). Audit read-only:
1. **Per lane** (intent, bind express, units, bolts per unit, sync, emit fsd): daftar file md yang benar-benar di-load (SKILL.md + references yang di-"Read" oleh langkah), ukuran byte, estimasi token. Pakai benchmark T02/T03 sebagai baseline dan verifikasi angkanya masih berlaku pasca-Fase 1–3.
2. **Duplikasi prose antar reference**: paragraf/aturan yang muncul ≥2 kali (routing-rules vs chain-execution vs SKILL.md orchestrate-flow adalah tersangka utama — 202 KB untuk satu skill).
3. **Instruksi yang bisa jadi tabel keputusan**: prose ≥3 paragraf yang sebenarnya satu tabel kondisi→aksi.
4. **Reference yang di-load tapi tidak dipakai langkah itu** (progressive disclosure bocor).
5. Rekomendasi: target token per lane, daftar file yang di-merge/dipangkas/dipindah, dan — penting — **mana yang tidak boleh dipangkas** karena dia rail anti-halu (Hard rule, halt taxonomy, verdict grammar). Jangan ulangi kesalahan kelas "carve-out prose": kalau sebuah aturan harus mekanis, jangan jadi prose yang lebih pendek, jadikan tabel/script.

Laporkan keduanya terpisah, masing-masing berhenti di gate. Urutan: A dulu (kecil), lalu B.
