# Fase 5 — tabel penempatan (BERHENTI DI TABEL; kolom "pertanyaan terbuka" = lo)

**Status: audit read-only, nol perubahan kode.** Konsumen = hasil grep hari ini (bukan prose). Verdict FINAL = sudah lo putuskan di gate7 (`2026-08-23-v7-gate7-accept-730.md`), tinggal eksekusi setelah tabel ini lo sahkan. Rumah mengikuti addendum placement. Baseline pengukuran = `context-v7.3.0-post-observability.json`; target Fase 5: pipeline 1-unit ≤120k tok.

| # | Permukaan (ukuran) | Konsumen nyata (grep) | Rumah tujuan | Tindakan | Status / pertanyaan terbuka |
|---|---|---|---|---|---|
| 1 | `skills/_vendored` 49 KB + `sync-vendored.sh` | Dirujuk PROSE oleh orchestrate (SKILL, chain-execution, routing-rules) + execute-bolts (SKILL preflight-1 "superpowers bridge", superpowers-bridge.md dispatch-order). TIDAK ada script yang membacanya | Hapus | HAPUS utuh; superpowers-bridge.md ditulis ulang: agent first-class = SATU-SATUNYA jalur dispatch (fallback vendored + halt `dep_missing` bridge-nya ikut hilang); inline-kan ≤ beberapa baris teknik TDD failing-first yang masih dikutip | **FINAL** (gate7). — |
| 2 | `skills/slice-design` + `commands/slice.md` + refs (≈40 KB) | Command surface sendiri; membaca `references/design-intelligence` (slice-procedure); tidak dibaca skill pipeline mana pun | `mega-sdd-extras` ATAU hapus | Rekomendasi gue: **hapus dulu** (buat `mega-sdd-extras` hanya kalau ada permintaan pemakai nyata — no-gimmick; git history menyimpan semuanya, mudah dihidupkan sebagai plugin kedua nanti) | **FINAL arah (gate7), pilihan extras-vs-hapus = gue → hapus.** Konfirmasi kalau lo mau extras sekarang |
| 3 | `references/design-intelligence` 164 KB | Lens design (via `context-enrichment`/`starterkit-enrichment` → design slice: `modern-baseline` + `style-principles`/ux) + generate-intent (`product-style-map.yaml` untuk Design-Source OQ recommend, generation-guide + vault-core §design_system) + slice-design (ikut #2) | Plugin (pangkas) | KEEP yang dibaca lens + Design-Source OQ (modern-baseline, style-principles, product-style-map); HAPUS sisa (typography-pairings, palette catalogs, dll.) setelah grep per-file | **TANYA:** Design-Source OQ recommend (generate-intent) tetap fitur? Kalau ya, product-style-map ikut KEEP |
| 4 | `skills/scan-codebase` lane klasik 184 KB (map emission, engine ladder, deep-scan cache, `probe-scan-engine.sh`, `derive-codebase-map.sh`, `validate-codebase-map.sh`) | `codebase-map` masih dirujuk LUAS: express-bind (fallback klasik + pack detection), bind refs (constitution-and-oq, hard-rules-and-packs), generate-intent `--scan=` (starterkit-first Mode A!), generate-units decomposition, chain routing (scan-first brownfield) | Plugin (bedah besar) | Lane klasik → hapus; pertahankan producer `reuse-index.yaml` + `symbol-index` + deep-scan starterkit (dipakai `--scan=` starterkit-first). CATATAN BUKTI: `--scan=` generate-intent + pack-detection bind MEMBACA map — "express needs no map" benar untuk BIND, tidak untuk INTENT starterkit-first | **TANYA (evidence-flip):** starterkit-first Mode A (map → dual-citation vault) ikut hidup atau mati? Kalau hidup, map producer minimal tetap; kalau mati, greenfield-only |
| 5 | `agents/phase-advisor.md` + `build-advisor-bundle.sh` + `--advisor`/`--no-advisor` + advisor-checklist ×2 | bind SKILL Step 2.12 scope gate + express-bind + generate-intent Step 3.7 + config; test 5c/p10 pin | Hapus | HAPUS utuh (agent, script, flag plumbing, checklist refs, patch advisor di deriver, repin test) | **FINAL** (gate7). — |
| 6 | `skills/install-deps` 42 KB + `detect-os.sh`/`probe-tool.sh`/`fix-windows-path.sh`/`_lib/windows_path.py` + tool-matrix | Prasyarat pipeline (ast-grep/playwright/pandoc/mmdc); Windows PATH repair = fleet kantor (lo konfirmasi di gate7: TETAP) | Plugin (pangkas) | Pangkas ke satu command: audit→plan→install→verify per OS; buang prose panjang (SKILL jadi router tipis di atas probe-tool/detect-os); PATH repair Windows tetap | **FINAL** (gate7). — |
| 7 | Maintainer refs: `fork-a-recovery-map.md`, `upgrade-from-old-version.md`, `reading-map.md` (±38 KB) | TIDAK di-Read skill saat run (grep: hanya prose rujukan silang); pembaca = manusia | `docs/` | PINDAH ke `docs/` + repoint rujukan; keluar dari paket references/ | **FINAL** (gate7: "reference maintainer → docs/"). — |
| 8 | `references/shared-snapshot-schema.md` 7 KB | **DIBACA saat run**: deep-scan-gate (writer), bind auto-memory-handoff (freshness attest), detect-drift auto-and-chain | Plugin | TETAP di references/ — bukti membalik verdict awal "pindah docs/" | Bukti dilaporkan; tidak perlu tanya |
| 9 | `references/lib-patterns` (django/laravel) 25 KB | Extractor deep-scan starterkit | Ikut nasib #4 | KEEP jika deep-scan starterkit tetap | Ikut jawaban #4 |
| 10 | `commands/update-plugin.md` + `migrate-paths.md` | migrate-paths = wajib selama dual-layout 1 minor; update-plugin = ops kecil | Plugin | KEEP sementara; review saat fallback layout-1 dicabut | **FINAL** (fase5 doc). — |
| 11 | Dirty journal `.dirty-paths.jsonl` | DIBACA `derive-changed-paths.sh` + `state_probes.py` + scan `--changed-only` (sync) | Plugin | TETAP | Sudah final di 7.3.0 |
| 12 | `mega-sdd-trace:*` | Filter gateway | Plugin (kontrak gateway) | SELESAI v7.3.1 (`docs/gateway-contract.md`) | Tutup |
| 13 | `publish-artifacts.sh` | Stop hook → gateway; payload = dokumen + plugin_version | Plugin | TETAP; verifikasi endpoint/payload dgn tim gateway saat field test | **TANYA gateway** (item lama, bukan blocker) |
| 14 | `--price-table` calculator | SUDAH DIHAPUS di 7.3.0 (amandemen eksplisit menamainya) | — (git history) | Kalau riset routing lapangan mau dihitung dari ekspor gateway, hidupkan sebagai alat di `benchmarks/` — keputusan terpisah | **TANYA:** perlu di benchmarks/? |
| 15 | Validator PostToolUse yang tidak dibaca gate (prinsip fase5 §2) | Perlu audit per-validator (aggregator PreToolUse sudah recompute-at-gate) | Plugin (pangkas) | Audit terpisah saat eksekusi Fase 5 (daftar validator → gate reader per grep) | Dikerjakan saat eksekusi |

**Urutan eksekusi yang gue usulkan (satu commit per permukaan, bisectable):** №5 advisor → №1 _vendored → №2 slice-design → №7 docs move → №3 design-intelligence (setelah jawaban) → №4 scan (setelah jawaban) → №6 install-deps → №15 validator audit → tracer ulang + bump **7.4.0**.

Jawaban yang gue butuh sebelum eksekusi: **№3 (Design-Source OQ), №4 (starterkit-first Mode A hidup/mati), №2 (extras sekarang atau hapus), №14 (price-table ke benchmarks?)**. Sisanya FINAL dan langsung jalan begitu tabel ini lo sahkan.
