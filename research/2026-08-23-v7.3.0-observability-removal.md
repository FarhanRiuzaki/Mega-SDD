# v7.3.0 — observability DIHAPUS TOTAL (audit uji pisau + hasil)

**Amandemen final user 2026-08-22** (menggantikan amend-no-telemetry): mega-sdd = pipeline development SAJA. Uji pisau: *"kalau ini hilang, adakah gate anti-halu yang bolong, atau artefak dev (vault/unit/bolt/test script/e2e/dokumen) yang tidak bisa dibuat?"* Tidak → hapus. Tidak ada pengecualian kontrak gateway.

**Status: SHIPPED 3 commit (`bfdf996` seri-1 telemetry/cost/advisor/trace · `447eb2b` seri-2 lane memory · seri-3 docs+bump+baseline), CI hijau, suite penuh dua tree per commit.** Catatan urutan: R4 selesai dengan angka SEBELUM seri ini (sesuai instruksi); R2a terlanjur mendarat sebelum amandemen terbaca — dilaporkan, tidak di-revert (hijau + terukur turun).

## Before → After

| Metrik | Before (`6b18c09`) | After | Δ |
|---|---|---|---|
| Baris hooks | 3.924 | 2.810 | **−1.114 (−28%)** |
| Event hooks | 8 (SessionStart, PreToolUse, PostToolUse, Stop, SubagentStop, UserPromptExpansion, UserPromptSubmit, PreCompact) | **5** (SubagentStop/UserPromptSubmit/PreCompact DIHAPUS) | −3 event |
| Scripts | 84 | 82 (−report-token-cost, −memory-write) | −2 |
| Skills | 22 | 21 (−memory) | −1 |
| Commands | 8 | **7** (4 verb + 3 one-timer; −memory) | −1 |
| Suite test | 236 | 226 (13 suite fitur-mati dihapus, 3 file test tambah/gabung) | −10 |
| Baris kode netto seri-1+2 | — | **−6.299** (372 insert / 6.671 delete) | |
| **Tracer 10 lane (chars÷4)** | 728.322 tok (pre-Fase-4) | **666.156** | **−62.166 (−8,5%)** — kumulatif R4+R2a+seri ini |

Spawn tier-S: S17 no-signal = 0 fork + **0 output** (marker governance ikut dihapus); S18 SDD+index tetap ≤12 (terukur 8); Stop non-SDD 0 fork; un-armed Write = 1 fork journal (semuanya di-pin `tests/weighted-routing/`).

## Dihapus (kelas → isi)

1. **Telemetry**: telemetry.jsonl + SEMUA writer/marker (turn_end/subagent_end/skill_invoked/ref_loaded/halt_self_resolved), ekstraksi usage transcript + cursor, hook-debug.log + rotasi, telemetry-schema.md, `--no-telemetry`/config `telemetry:`/userConfig A7. Opt-out `telemetry: false` yang dulu MEMATIKAN gate/validator ikut mati → **gate tidak bisa di-opt-out via config lagi** (breaking, disengaja).
2. **Cost/usage**: report-token-cost.sh (termasuk `--price-table` yang baru sehari), leg analyze, TOKEN-COST-REPORT; analyze kehilangan trigger "token cost".
3. **Advisor runtime**: compaction advisor + PreCompact snapshot + notice resume; staleness notice **TETAP** (state pipeline: kode bergerak sejak scan — keputusan tercatat).
4. **Trace tag `mega-sdd-trace:*`**: 15 announce SKILL, aturan using-mega-sdd, builder dispatch + template + lens rule, echo session-start/UPS; **kontrak governance v6.19.2 dicabut dari plugin** — deteksi sesi = urusan gateway. Goldens dispatch-parity di-regen 2× (delta = baris tag + section memory saja).
5. **Lane memory**: skill memory + command + memory-write.sh + migrations; §Memory layer di 10+ surface; memory_context/memory_writes dari handoff; routing preflight + chain-end write + extract-learnings; instincts; drift-history; classifier-accuracy; install-outcomes (install-deps re-probe tiap run); scope-picker → default dari vault `prd_sha256`; recommendation-context → KB→vault→codebase; ground.sh Guards 3+9.

## Konsumen pipeline yang DIPERTAHANKAN (dilaporkan, sesuai mandat)

- **`<vault>/.memory/bolt-outcomes.json`** — DIBACA SCRIPT (`query-graph.sh --modules` = status modul/DoD; diagnostics) = state pipeline; spec tulis minimal direstorasi (append langsung; field learning dihapus). Path tidak dipindah (rail "yang dibaca script tidak disentuh").
- **Konvergensi `--converge`** — tetap hidup dari sumber grounded; flag RENAME `--auto-accept-from-memory` → `--auto-accept` (breaking).
- **`halt_auto_propose`** — config pipeline; path user-scope relokasi ke `~/.mega-sdd/config.yaml`.
- **Halt `memory_in_use`** — kelas = lock konkurensi vault.json/starterkit (nama historis); spec lock kanonik = vault-core §Concurrency contract (implementasi: derive-vault-json.sh). Dicabut hanya dari enum generate-units.
- **Dirty journal** — TETAP (changed-set untuk sync = pipeline). **publish-artifacts.sh** — TETAP (payload dicek: manifest `plugin_version` saja, tanpa telemetry). **Front-door status view** — TETAP. **Tracer `benchmarks/`** — TETAP (alat maintainer, tidak jalan saat dev pakai plugin).

## Zero-phantom + tests

Sweep tanpa filter ekstensi di kedua tree + hooks.json + CLAUDE.md + README + references: sisa kemunculan kata hanya (a) komentar "removed in v7.3.0" di titik bekas, (b) nama path `.memory/bolt-outcomes.json` (konsumen script), (c) `05-decisions.md`/`vault.md ## Decisions` (nama dok vault legacy — bukan lane memory), (d) sed legacy-alias di migrate-paths (migrasi konten proyek lama). 13 suite fitur-mati dihapus; ~20 suite di-repin ke kontrak removal (assertion negatif: tag/section/hook TIDAK boleh ada); cascade/builder-shape ditulis ulang ke kontrak "legacy outcomes.md DIABAIKAN". Suite penuh dua tree hijau per commit.

Baseline Fase 4 BARU: `benchmarks/results/optimized/context-v7.3.0-post-observability.json` (R2b dst diukur terhadap ini). Checkpoint Fase 4 (evidence-stop R2a + opsi (i)/(ii)/(iii)) tetap menunggu keputusan gate.
