# v8 program §1 — P0 measurement report (sesi 1, 2026-09-10)

**Status:** **BLOCKED pada host permission — dua arm live BELUM JALAN; kill-criterion BELUM DIPUTUSKAN (tidak ditebak).** Yang selesai di fase ini: (a) fixture dua arm siap + launcher headless tervalidasi, (b) run brownfield live 10 kelas CONFLICT lewat jalur JIT = **9/11 CONFLICT, 0 CONFIRMED-by-absence (MEASURED, n=1)**, (c) keputusan owner final diterapkan di kode/dok (spec F10), (d) semua angka di bawah berlabel.
**Kontrak:** `research/2026-09-10-v8-autonomous-runbook.md` §1. **Runbook asli:** `research/2026-09-10-p0-baseline/README.md`.

## 1. Apa yang memblokir arm live (dan satu perintah yang membukanya)

Arm P5 headless dijalankan lewat `claude -p` di clone fixture (owner tidak standby). Dua percobaan launch ditolak oleh **auto-mode permission classifier Claude Code** di sesi runner — bukan oleh plugin:

| Percobaan | Bentuk | Hasil |
|---|---|---|
| 1 | `claude -p … --dangerously-skip-permissions` | DENIED (bypass global) |
| 2 | `claude -p … --permission-mode acceptEdits --allowedTools "Bash,Read,Write,Edit,…,Skill,Agent"` via `nohup` (launcher `benchmarks/scripts/p0-headless-run.sh`) | DENIED — alasan classifier: *"launches a detached headless coding-agent loop with Bash allowed … no per-action approval gate; run this step outside auto mode so the permission prompt can be reviewed"* |

Rute yang TIDAK sah dan tidak dipakai: menjalankan chain lewat `Agent` sub-agent (memory `moat-token-tradeoff`: Agent-offload MEM-BYPASS gate → bukan jalur produksi, angkanya tidak valid); menjalankan chain di cwd repo plugin (hook memakai cwd sesi → mencemari repo).

**Yang membuka blok (satu baris, dari prompt owner — `!` menjalankan di sesi owner sehingga prompt izin muncul SEKALI ke owner):**

```
! bash benchmarks/scripts/p0-headless-run.sh /Users/farhanriuzaki/SunnyGo/2026/AIRND2026/Project/TRAINING/p0-xs-arm PRD/prd-company-profile.md benchmarks/results/p0-baseline/xs-3screen
```

lalu untuk klinik:

```
! bash benchmarks/scripts/p0-headless-run.sh /Users/farhanriuzaki/SunnyGo/2026/AIRND2026/Project/TRAINING/p0-clinic-arm PRD/prd-clinic.md benchmarks/results/p0-baseline/clinic
```

Setelah keduanya selesai (proses `claude -p` keluar; `stream.jsonl` berhenti bertambah), ekstraksi deterministik — siapa pun:

```
python3 research/2026-08-04-p5-extract.py <transcript dari run.meta> <ISO commit unit pertama> [<ISO commit unit terakhir>] --json benchmarks/results/p0-baseline/<arm>.json
```

(`git -C <arm> log --format="%h %cI %s"` memberi ISO-nya.) Verdict kill-criterion kemudian ditulis dari `pre_code_share` di JSON itu, per aturan §1: ≥25 % ⇒ P2; <25 % ⇒ program berhenti di P1.

## 2. Protokol yang dipakai (deviasi dari P5 interaktif, dinyatakan)

| Aspek | P5 interaktif (runbook asli) | Headless (launcher ini) | Dampak pada angka |
|---|---|---|---|
| Penjawab ask | owner, tempo natural (human-wait dikurangkan) | **`AskUserQuestion` NONAKTIF di `-p`** (diprobe 2026-09-10: `No such tool available: AskUserQuestion … disabled for this session, in subagents as well as here`) → model diberi system prompt: pilih opsi paling konservatif sendiri, tulis `[ASSUMED-BY-RUNNER: …]`, lanjut | `interaction_points` = jumlah PERCOBAAN ask (tool_use `AskUserQuestion` yang error) — tetap terhitung; human-wait = 0 by construction; `idle_ratio` = waktu model+tool saja |
| Izin | interaktif per aksi | allowlist eksplisit (`--allowedTools`), bukan bypass global | tidak mengubah jalur gate (hook tetap jalan) |
| Model | `/model opus` | `--model opus` (probe: `claude-opus-5`) | paritas dengan dua arm P5 |
| Flag chain | tidak ada | tidak ada (prompt polos: *"jalankan mega-sdd dari PRD/… sampai semua unit selesai (DONE), lalu /mega-sdd:analyze"*) | default 7.34.0 apa adanya = arm baseline |
| Fixture | clone `training-nextjs` @ `c6821ad` + PRD | sama; `npm install` di luar clock (node_modules disalin dari `p5-express-arm`, `package-lock.json` sha identik `8b3f7c50255e`) | — |
| Transkrip | `~/.claude/projects/<cwd>/<sid>.jsonl` | sama (`--session-id` tetap; ekstraktor v2 terbukti membaca transkrip headless: `records=37 models={'claude-opus-5': 7}` pada probe) | ekstraksi identik |

Fixture (MEASURED `derive-project-scale.sh`): `p0-xs-arm` PRD `prd-3screen-xs.md` → `{"project_scale":"xs","screens":3,"entities":1,"flows":1}`; `p0-clinic-arm` PRD `sample-prd-clinic.md` → `{"project_scale":"standard","screens":7,"entities":4,"flows":6}`. Kedua arm: commit setup `bench: seed PRD` (di luar clock), HEAD sebelum run = `6f98c10` (xs) / `b915556` (klinik). Plugin user-scope di-update **7.30.0 → 7.34.0** (`claude plugin marketplace update` + `claude plugin update`), dicek `claude plugin list`.

## 3. Run brownfield live — 10 kelas CONFLICT simkredit lewat JIT (MEASURED, n=1)

Tujuan: menutup batas replay 7.33.0 (8 kelas konten dibuktikan lewat kontrak OUTPUT ladder saja). Di sini ladder E3 **dieksekusi live** (`bind-codebase/references/express-bind.md §Step E3`, rung 1→6 verbatim) terhadap kode brownfield NYATA — clone `training-nextjs` @ `3cec603` (pasca bolt U-007 prisma) di `TRAINING/p0-brownfield-replay`. Eksekutor ladder = model runner (Claude Fable 5.1) dalam peran controller execute-bolts pre-flight 3.9 — **bukan opus**, disclosed. KB absen (rung 5 n/a). Symbol index dibangun `build-symbol-index.sh` (603 simbol / 314 file); `.prisma` di luar cakupan index → klaim entitas jatuh ke rung 2–4 (Read + grep terbatas), persis alasan rung 4 ada.

Jalur: `derive-unit-claims.sh` (`fs_claims=3 symbol=0 text=8`) → `write-unit-binding.sh` (fs by script) → ladder E3 → `--verdicts` → `validate-handoff-binding-units.sh --units=U-001`. Raw: `benchmarks/results/p0-baseline/brownfield-replay/` (unit, wave-claims, ladder-verdicts, binding.json, validation-blockers, run.meta).

| Klaim | Kelas lapangan | Verdict live | State | Anchor (READ) | Bukti ringkas |
|---|---|---|---|---|---|
| C-01 | User created_at hilang | **CONFLICT** | PARTIAL_FIELDS_MISSING | schema.prisma:40 | model dibaca penuh :40-60, tanpa created_at; header :25-26 "hanya product_audit_logs" |
| C-02 | Product created_at hilang | **CONFLICT** | PARTIAL_FIELDS_MISSING | schema.prisma:74 | LoanProduct :74-107 tanpa created_at |
| C-03 | Simulation created_at hilang | **CONFLICT** | PARTIAL_FIELDS_MISSING | schema.prisma:108 | :108-151 hanya deletedAt |
| C-04 | Installment created_at hilang | **CONFLICT** | PARTIAL_FIELDS_MISSING | schema.prisma:152 | tidak ada model Installment (grep terbatas: hanya nama kolom); SimulationSchedule :152-174 tanpa created_at |
| C-06 | money BigInt vs Decimal(18,2) | **CONFLICT** | IMPLEMENTED | schema.prisma:95 | plafon/installment/interest/balance = BigInt (rupiah bulat, OQ-DM-4 :93); satu-satunya Decimal = annualRate (5,2) :87 |
| C-07 | enum SimulationStatus 2 vs 3 | **CONFLICT** | PARTIAL_FIELDS_MISSING | schema.prisma:108 | schema dibaca penuh: tidak ada `enum`; Simulation tanpa kolom status; grep status silent |
| C-20 | username vs nip | **CONFLICT** | IMPLEMENTED | auth.ts:66 | authorize POST `{username,password}` :58-66; leg collision: `nip` hanya di prisma User :43 |
| C-21 | endpoint refresh tak ada di kontrak | CONFIRMED (fs, script) | NEW | — | `apps/api/src/auth/refresh.ts` ABSEN di commit ini — must-not-exist terpenuhi (fixture mini 7.33.0 sengaja menanamnya) |
| C-22 | halaman register harus dihapus | **CONFLICT** (fs, script) | ALREADY_EXISTS | register/page.tsx | file ada |
| C-23 | JWT arrays vs single role | **CONFLICT** | IMPLEMENTED | auth.ts:105 | jwt callback menyimpan roles[] + permissions[] :105-106; jwt-permission Set<string> :31-35; leg collision: prisma User.role String tunggal :50 |
| (A01) | target_files modify | CONFIRMED (fs, script) | IMPLEMENTED | schema.prisma | must-exist |

**Hasil:** `summary {CONFIRMED: 2, CONFLICT: 9, OQ: 0}`; gate `--units=U-001` → **FAIL, 9 drop `conflict_unresolved`**, `jit_units [{U-001, listed, conflicts_open 9}]`. **8/8 klaim konten → CONFLICT oleh ladder live; 0 CONFIRMED-by-absence; 0 OQ.** Dua CONFIRMED adalah kebenaran filesystem yang benar (C-21: refresh.ts memang tidak ada di commit ini — kelas lapangan itu ada karena bolt lama membuatnya; di HEAD fixture kelasnya sudah tertutup). Batas yang tersisa: n=1, eksekutor bukan opus, dan ini pengukuran *judgment*, bukan wall-clock.

## 4. Keputusan owner final yang diterapkan (commit `16bf16e`)

- xs body **5,4:1 = contract floor**, target F1(e) diamandemen "≤4:1 atau contract floor terdokumentasi", **F1(e) CLOSED** (spec §3 / App. C / F1 / F10).
- Tiga halt DEFER-loud (spec F10, audit §E-6 ditutup): `review_critical_unresolved` → **DEFER/karantina**; `bolt_introduces_locked_drift` **TETAP BLOCKING** (CONFLICT-like: entitas LOCKED = klaim spec terkunci manusia; override-only, never propose-and-confirm — kontradiksi audit §D-2 ditutup ke arah konservatif); `constitution_drift_detected` **TETAP BLOCKING di lane detect-drift** (hash constitution ≠ hash binding = grounding hard-rule basi). `[ASSUMED-BY-RUNNER: dua halt drift diklasifikasi CONFLICT-like — keduanya mempertahankan perilaku hari ini, tidak ada gate dilonggarkan]`. Registry tetap 33.981 B (cap 34.000, baris ditulis ulang byte-neutral); bolts.md 13.983 B (cap 14.000).

## 5. Kill-criterion

**BELUM DIPUTUSKAN.** Aturan §1: verdict hanya dari `pre_code_share` MEASURED di dua arm. Angka pembanding yang ada tetap P5 klasik 5.9.0 (pra-kode 155 m vs bolt-1 43 m ⇒ 78 %) — bukan 7.34.0, tidak dipakai sebagai verdict. P2 **tidak dimulai** sampai §1 ada angkanya.

## 6. Yang terkumpul di sesi ini

Commit (leg GitHub; leg scm ikut bila VPN aktif — cek `git ls-remote` kedua leg): `5388fcd` runbook verbatim · `16bf16e` keputusan owner · (rilis 7.34.1: launcher + hasil replay + laporan ini). **SCM PENDING:** lihat baris terakhir laporan sesi / `git ls-remote` — saat sesi ini scm resolve (10.190.1.2) dan push dua leg berhasil untuk 0ba6e72.

NEXT SESSION: jalankan dua baris `!` di §1 (izin muncul sekali ke owner), tunggu selesai, lalu paste: *"lanjut §1: ekstraksi dua arm + verdict kill-criterion dari `benchmarks/results/p0-baseline/`"*.
