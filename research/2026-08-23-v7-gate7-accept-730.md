# v7.3.0 — ACCEPTED. Lanjut Fase 5 (pipeline-only) dengan addendum placement

Diterima: hooks 8→5 event, −6.299 baris, tracer −62k (−8,5%) sebagai baseline baru, tier S tetap 0 fork, moat tak tersentuh. R2a yang terlanjur mendarat tidak perlu di-revert (hijau + terukur turun). Efek samping `telemetry: false` yang dulu ikut mematikan gate — bagus itu mati; gate memang tidak boleh bisa di-opt-out lewat config.

Checkpoint Fase 4 sudah gue putuskan di `research/2026-08-22-v7-gate6-fase4-checkpoint.md`: prose-diet DITUTUP di R4+R2a; lever berikutnya = hapus permukaan (Fase 5) lalu script-ification (Fase 6). Tidak ada R2b–R3.

## Sekarang: Fase 5

Jalankan `research/2026-08-22-v7-fase5-pipeline-only.md` **dengan** `research/2026-08-22-v7-addendum-placement.md`: untuk tiap permukaan, kolom rumah tujuan (plugin / `benchmarks/` / `docs/` / gateway / `mega-sdd-extras` / hapus) + konsumen nyata (grep) + pertanyaan terbuka. Berhenti di tabel; gue isi kolom pertanyaan terbuka (trace-tag sudah terlanjur dihapus — kalau tim gateway ternyata butuh, itu jadi item "kontrak gateway" terpisah nanti, bukan revert).

Verdict yang sudah bisa lo anggap final dari gue (tidak perlu tanya): memory lane = sudah dihapus; `_vendored` → hapus (inline ≤ beberapa baris kalau ada instruksi yang masih dipakai); `slice-design` + `design-intelligence` non-lens → `mega-sdd-extras` kalau lo mau buat plugin kedua, kalau tidak → hapus; phase-advisor → hapus; scan klasik → hapus, pertahankan reuse-index + symbol-index producer; reference maintainer → `docs/`; install-deps → pangkas ke satu command (Windows PATH repair: tetap — fleet kantor Windows).

Bump 7.4.0 di akhir Fase 5. Lalu Fase 6 audit script-ification (gate). Leg scm gue push dari VPN.
