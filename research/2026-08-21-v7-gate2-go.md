# Fase 1 — ACCEPTED. Lanjut Fase 2 (script & hook diet)

Bukti Fase 1 gue terima: tier S terukur 0 fork di PreToolUse/PostToolUse/UserPromptSubmit, armed path identik pre-v7 (mutation-proof S12), subagent fail-closed lewat sentinel `agent_id` (S13). Lima deviasi di proof §4 semuanya masuk akal dan gue setujui — khususnya #1 (armed = `chain_engaged` saja, tanpa AND factory-ledger; lubang first-chain greenfield memang nyata) dan #5 (handoff gate-time absent-only dulu; full recompute + content-hash wajib mendarat di Fase 2 SEBELUM leg Stop dihapus).

Satu hal sebelum mulai: tempel output `gh run list --limit 3` untuk commit `9017dc9` — gue mau lihat CI hijau di kedua tree, bukan hanya lokal.

## Fase 2 — scope & urutan

Sumber kebenaran: audit `research/2026-08-21-v7-diet-audit.md` §6.2–6.6 + keputusan `research/2026-08-21-v7-gate0-decision.md` + backlog `research/2026-08-21-v7-phase1-proof.md` §5. Jangan buka diskusi ulang item yang sudah diputuskan; kalau implementasi menemukan bukti baru yang membalik keputusan, berhenti dan laporkan, jangan putuskan sendiri.

Urutan commit (satu commit per langkah, test hijau KEDUA tree + `.github/workflows` per commit, `gh run list` sesudah push):

1. **Hooks backlog (proof §5)** — selagi konteks hook masih segar:
   - C1 self-resolve 9-guard: session-start → `ground.sh`. Session-start tersisa: install-front-door (debounced), derive-state untuk satu baris notice, inject anchor. Session-start tidak menulis artifact vault. Ukur ulang spawn session-start (before ±22–26) dan kunci di test.
   - Handoff gate-time: absent-only → full recompute + content-hash di state validator (dedup `retry_count`), lalu hapus leg Stop. Dua langkah ini satu commit supaya tidak ada jendela tanpa penjaga.
   - Rotasi `hook-debug.log`; collapse bash staleness fallback ke `derive-state.sh`.
   - Re-run `tests/weighted-routing/test-tier-s-hooks.sh` setelah setiap commit hook — angka ceiling tier S tidak boleh naik.

2. **DELETE batch (§6.2: 16 script + `seeding_budget.py`)** — boleh satu commit, dengan sweep per item persis seperti audit (kedua tree, fixtures, prose-rot yang disebut §6.6). Rung hook milik validator yang dihapus ikut hilang di commit yang sama (pandoc ±65L, starterkit ×2 + producer `deep-scan-dispatch.md` + `starterkit-context-schema.md`). Zero-phantom grep sesudahnya: nol referensi nama file terhapus di seluruh repo.

3. **DEMOTE 3 (§6.3)** — relocate-then-delete, satu commit.

4. **MERGE 10 grup (§1.4)** — SATU GRUP PER COMMIT. Nama state-file yang dibaca aggregator PreToolUse dan `run_family` run-analyze tidak berubah, atau diupdate di commit yang sama. Mulai dari yang paling terisolasi (sync-vendored, derive-binding-json+stamp, validate-new-deps+check-dep) lalu yang berat (validate-kb terpadu, pack-driven driver, preflight).

5. **`build-dispatch-prompt.sh`**: keluarkan ±350–500L prose statis ke template markdown yang dibaca script; ekstraksi `_lib/unit_parse.py` dari 4 kopi yaml_lite — ter-gate dispatch parity harness. Tidak mengubah byte output dispatch prompt (buktikan dengan diff sebelum/sesudah pada fixture).

6. **Tier B per gate-0**: token-cost kurus (satu report, hapus marker yang tidak dibaca), vault-flow-staging fix header, memory lane tidak disentuh kecuali ada rung per-turn, analyze-parallelism tetap.

7. **CI diet** (sudah approved): path filter docs-only, cache npm, paralel loop suite HANYA setelah bukti tidak ada suite yang menulis state repo bersama.

## Rambu

- Tidak ada script baru netto. Tidak ada rename state-file. Tidak menyentuh vault (Fase 3).
- Setiap commit: pesan jelas, CHANGELOG 7.0.0 tumbuh, README/`CLAUDE.md`/references yang menyebut file terhapus ikut diperbaiki di commit yang sama.
- Bersihkan artefak runtime di working tree repo ini (`.mega-sdd/memory`, `plugins/mega-sdd/.mega-sdd/`, `.playwright-mcp/`, `package.json`/`node_modules` dari mega-code kalau bukan bagian repo) — kalau memang perlu, masukkan ke `.gitignore`, jangan ikut ter-commit.

## Penutup Fase 2 (gate)

Laporkan: tabel before/after (jumlah file & baris script, python, hook), daftar file terhapus/merge dengan alasan satu baris, hasil test kedua tree + CI link, dan ulangi trace tier S (spawn terukur) untuk membuktikan diet tidak meregresi Fase 1. Lalu berhenti — Fase 3 (vault) punya gate sendiri dengan contoh dari `sample-prd-clinic.md`.
